version: "1.0.0"
title: "builder-II governed read-only session"
description: >
  Governed interposition recipe (G2). Goose's only tool surface is the builder-II governed
  MCP server; there are no developer/shell builtins. Every tool call is mediated by
  `builder-mcp serve`, which runs the governed envelope -> receipt -> ledger ceremony and
  exposes only read-only tools. This recipe grants no authority and enables no write,
  shell, or target mutation.
instructions: |
  All tool calls are mediated by the builder-II governed MCP server (extension
  "builder_ii_governed"). Only read-only tools are available: there is no shell, no file
  editing, and no target-repo mutation. Every call is recorded as a governed receipt and a
  hash-chained event; nothing here grants execution or promotion authority.

  Available tools:
    - list_dir(path)            orient in the tree; directories are suffixed "/"
    - grep(pattern, path)       find a literal substring, returned as "path:line:text"
    - read_file(path)           read a UTF-8 text file
    - echo(text), utc_static()  deterministic stubs

  All paths are relative to the repository root and jailed to it: absolute paths, "..", and
  the .git/.builder directories are refused. Results are bounded (files by bytes, listings and
  searches by count), and a truncated result says so on its last line -- narrow with `path`
  rather than assuming you have seen everything.

  You cannot write files, run shell, or mutate the target. If a change is warranted, do not try
  to work around the absence of those tools -- call propose_patch with `path` and `unified_diff`.
  That writes nothing: it records the change as a governed patch proposal for a human to review
  and approve or refuse. Say plainly that you have proposed it and what it would do.

  If a task requires shell, network, or anything else with no governed tool here, stop and report
  that it is out of scope for this session.
prompt: |
  Governed read-only session ready. Only the builder-II governed MCP tools are available.
settings:
  temperature: 0.0
  goose_mode: auto
extensions:
  - type: stdio
    name: builder_ii_governed
    cmd: builder-mcp
    args:
      - serve
    timeout: 300
    description: "builder-II governed MCP server (read-only repo tools; path-jailed; ledgered)"
