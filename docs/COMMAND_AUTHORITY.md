| Command Name | Tier | State | Runtime Boundary | Write Boundary | Approval Mode | Approval Boundary | Allows Shell | Process Control | Allows Writes | Artifact Writes | State Writes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `builder` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Delegates to helper subcommands; root CLI does not execute direct agent/model loops. | No direct write authority at root CLI level. | `explicit_operator_invocation` | Operator must explicitly run command options from active terminal. | No | No | No | No | No |
| `builder tui` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder hitl` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder profile` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder model` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder promote` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder postflight` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder goose` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder code-vault` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder hitl status` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder hitl chain` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder hitl pending` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder hitl approval` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder hitl evidence` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder hitl execution` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder hitl promote` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder hitl replay` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder profile status` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder profile lifecycle` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder profile validate` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder profile render-plan` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder profile dry-run` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder profile resolve` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder profile history` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder model routing show` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder model routing simulate` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder model routing candidates` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder model routing policy` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder model routing execution-policy` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder model routing validate` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder model registry show` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder model registry diff` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder promote status` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder promote readiness` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder promote artifact` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder promote decision` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder promote compatibility` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder promote history` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder promote gates` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder postflight status` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder postflight record` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder postflight verify` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder postflight governance` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder postflight actions` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder postflight refs` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder postflight validate` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder goose status` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder goose manifest` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder goose links` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder goose actions` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder goose governance` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder goose validate` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder goose approval` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder code-vault status` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder code-vault frame` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder code-vault determinism` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder code-vault recall` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder code-vault lint` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder code-vault context` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder code-vault governance` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder code-vault validate` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | No | No | No | No | No |
| `builder stratum` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Reads passive governance artifacts to render a spatial TUI; delegates execution to core tools. Refuses to launch unless invoked with the required --experimental flag. Command tier evaluation is real: it asks check_command_authority rather than guessing from the tier. No chain digest is displayed, because verify_artifact_chain exposes none; the surface renders an explicit absence marker instead of a synthesized value. It starts exactly one runtime, and never itself: the Goose keybinding suspends the TUI and hands the operator's terminal to builder-goose start-readonly with a fixed argv and shell=False, so that command applies its own read-only policy, emits its own launch and close receipts, and runs its own no-mutation postflight. It fails closed twice before anything spawns -- enforce_command_authority must permit the governed command, and a manifest its own validator accepts must already request read_only, which STRATUM never mints. It never spawns Goose directly and never selects Goose builtins. HITL approve/reject never mutate approval state and are not pending features: a surface that renders a digest must not harvest its confirmation, so they refuse and name the governed CLI. The HITL diff viewer remains an unimplemented mockup. | No direct write authority at TUI render level. The prepare-package and subagent-dispatch keybindings collect operator input and then refuse, naming builder-session prepare-package and builder-deepagents assign-subagent; the TUI emits no artifact and reports no dispatch it did not perform. Receipts under .builder/receipts are written by the governed command it invokes, never by the render surface. | `explicit_operator_invocation` | Operator must explicitly launch TUI with --experimental. No keybinding originates authority: every action that would write, approve, or dispatch is a constitutive refusal that names the governed command, and the one runtime it starts is started by that governed command in the operator's own terminal, never by the TUI harvesting a confirmation for it. It executes nothing else and claims no execution: the command palette is a tier-permission inspector, and the CLI passthrough is a context-injecting composer that surfaces a command for the operator to run in their own terminal. | No | No | No | No | No |
| `builder-runtime` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Interacts with local server endpoints, background agents, and runtime indicators. | Writes session runtime lockfiles and state indicators locally. | `explicit_operator_invocation` | Operator must trigger control signals manually. | No | No | No | No | Yes |
| `builder-runtime status` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Inspects local server endpoints, marker files, and listener processes using read-only probes. | No changes to workspace, runtime marker, or target repository. | `explicit_operator_invocation` | Explicit operator invocation only; no autonomous runtime probing. | No | No | No | No | No |
| `builder-runtime clear-marker` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Does not touch live processes; only clears the local runtime marker artifact. | Deletes or rewrites the builder runtime marker under configured local state paths. | `explicit_operator_invocation` | Explicit operator invocation only. | No | No | No | No | Yes |
| `builder-runtime stop` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Stops local runtime listener processes selected by the configured port and marker heuristics. | Clears local runtime marker unless --keep-marker is used; does not mutate source repositories. | `explicit_operator_invocation` | Explicit operator invocation only; foreign process termination requires literal confirmation. | No | Yes | No | No | Yes |
| `builder-runtime reset` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Composes stop plus marker cleanup for the local runtime listener. | Clears local runtime marker after attempted stop; does not mutate source repositories. | `explicit_operator_invocation` | Explicit operator invocation only; foreign process termination requires literal confirmation. | No | Yes | No | No | Yes |
| `builder-lanes` | Tier 1 — artifact-only planning/validation | `validation_only` | Evaluates passive rules and checklists; no subprocess execution. | No changes to workspace. | `none` | Passive check, no approval required. | No | No | No | No | No |
| `builder-tools` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Delegates governed external tool execution subcommands. | No direct write authority at root CLI level. | `none` | Delegated to subcommands. | No | No | No | No | No |
| `builder-tools list` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Runs local PATH/version probes for known external developer tools. | No changes to workspace, target repository, or external tool state. | `explicit_operator_invocation` | Explicit operator invocation only. | No | No | No | No | No |
| `builder-tools check` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Runs bounded local version probes for known external developer tools. | No changes to workspace, target repository, or external tool state. | `explicit_operator_invocation` | Explicit operator invocation only. | No | No | No | No | No |
| `builder-tools missing` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Runs bounded local version probes to identify missing required tools. | No changes to workspace, target repository, or external tool state. | `explicit_operator_invocation` | Explicit operator invocation only. | No | No | No | No | No |
| `builder-context` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Legacy context builder; delegates execution details to subcommands. | No direct write authority at root CLI level. | `explicit_operator_invocation` | Legacy context generator; operator must run command explicitly. | No | No | No | No | No |
| `builder-git-state` | Tier 1 — artifact-only planning/validation | `artifact_only` | Executes local git queries via read-only subprocess. | Writes declarative state file artifacts in workspace. | `none` | Passive git branch check. | No | No | No | Yes | No |
| `builder-targets` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Delegates target operations to helper subcommands; root CLI does not execute direct agent loops. | No direct write authority at root targets CLI level. | `explicit_operator_invocation` | Operator must explicitly run command options from active terminal. | No | No | No | No | No |
| `builder-targets list` | Tier 0 — read-only inspection | `spec_only` | Retrieves target profile metadata list; no execution. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-targets show` | Tier 0 — read-only inspection | `spec_only` | Retrieves target profile details; no execution. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-targets validate` | Tier 0 — read-only inspection | `spec_only` | Validates target profile registry or artifact. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-targets artifact` | Tier 1 — artifact-only planning/validation | `spec_only` | Resolves target details without execution. | Writes target profile JSON artifact to path. | `none` | None. | No | No | No | Yes | No |
| `builder-targets demo` | Tier 0 — read-only inspection | `spec_only` | Retrieves target profile demo recipe; no execution. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-targets readonly-founder-demo` | Tier 1 — artifact-only planning/validation | `spec_only` | Generates passive read-only founder demo planning artifacts; no execution. | Writes demo artifacts, events, and status JSON files in output directory. | `none` | None. | No | No | No | Yes | No |
| `builder-session` | Tier 1 — artifact-only planning/validation | `validation_only` | Delegates packaging and checks to subcommands. | No direct write authority at root CLI level. | `none` | Read-only checks or artifact-only packaging; no approval needed. | No | No | No | No | No |
| `builder-code-vault` | Tier 1 — artifact-only planning/validation | `artifact_only` | Governed read-only CodeVault frame, lint, recall, context projection, deterministic bench, and validation; no shell, model, or CORE runtime. | Writes explicit artifact JSON paths only; no target-repo mutation. | `none` | Operator initiates scan and reviews emitted artifacts. | No | No | No | Yes | No |
| `builder-agent` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Resolves active agent profiles and manifests. | No changes to workspace. | `explicit_operator_invocation` | Operator triggers agent inventory check. | No | No | No | No | No |
| `builder-bridge` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Tests readiness of external deepagents integrations. | No changes to workspace. | `explicit_operator_invocation` | Operator checks network bridge connectivity. | No | No | No | No | No |
| `builder-bundle` | Tier 1 — artifact-only planning/validation | `artifact_only` | Validates bundle definitions or packages build artifacts. | Creates ZIP or tar bundles in designated build folder. | `none` | Artifact build task. | No | No | No | Yes | No |
| `builder-goose` | Tier 1 — artifact-only planning/validation | `validation_only` | Delegates validation and read-only audits to subcommands. | No direct write authority at root CLI level. | `none` | Validation and audit only. | No | No | No | No | No |
| `builder-records` | Tier 1 — artifact-only planning/validation | `artifact_only` | Decodes and validates cryptographically signed or structured approval logs. | Writes verified signature files. | `none` | None; read-only verification. | No | No | No | Yes | No |
| `builder-preflight` | Tier 1 — artifact-only planning/validation | `validation_only` | Runs local environment checks (Python version, CLI presence). | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-receipt` | Tier 1 — artifact-only planning/validation | `artifact_only` | Generates or reads execution receipts. | Writes receipt JSON files to output folders. | `none` | None. | No | No | No | Yes | No |
| `builder-chain` | Tier 1 — artifact-only planning/validation | `validation_only` | Traces artifact lineage chains. | Writes chain validation artifacts. | `none` | None. | No | No | No | Yes | No |
| `builder-handoff` | Tier 1 — artifact-only planning/validation | `artifact_only` | Aggregates verified evidence and creates handoff bundle metadata. | Writes handoff markdown files. | `none` | None. | No | No | No | Yes | No |
| `builder-intake` | Tier 1 — artifact-only planning/validation | `artifact_only` | Ingests inputs from outside workspace. | Writes configuration files in specific workspace location. | `none` | None. | No | No | No | Yes | No |
| `builder-index` | Tier 1 — artifact-only planning/validation | `artifact_only` | Tracks and indexes generated artifact files. | Updates local artifact index JSON ledger. | `none` | None. | No | No | No | Yes | No |
| `builder-promotion` | Tier 1 — artifact-only planning/validation | `validation_only` | Analyzes readiness for target promotion. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-promotion-decision` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a signed promotion decision artifact. | Writes promotion decision JSON artifact. | `none` | None. | No | No | No | Yes | No |
| `builder-state-index` | Tier 1 — artifact-only planning/validation | `artifact_only` | Constructs system state summaries. | Writes state index JSON files. | `none` | None. | No | No | No | Yes | No |
| `builder-snapshot` | Tier 1 — artifact-only planning/validation | `artifact_only` | Records current directory snapshot hashes. | Writes workspace snapshot hash index. | `none` | None. | No | No | No | Yes | No |
| `builder-deepagents` | Tier 4 — forbidden/unpromoted automation | `forbidden_unpromoted` | Delegates deepagent specs rendering and validation to subcommands; active run is forbidden. | No direct write authority at root CLI level. | `forbidden_unpromoted` | Forbidden; no supported approval path. | No | No | No | No | No |
| `builder-notes` | Tier 1 — artifact-only planning/validation | `artifact_only` | Verifies or creates handoff notes artifacts. | Writes handoff notes markdown files. | `none` | None. | No | No | No | Yes | No |
| `builder-quality` | Tier 1 — artifact-only planning/validation | `validation_only` | Checks code linting or test coverage thresholds. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-research` | Tier 1 — artifact-only planning/validation | `artifact_only` | Builds read-only research plans. | Writes plan metadata files. | `none` | None. | No | No | No | Yes | No |
| `builder-performance` | Tier 0 — read-only inspection | `validation_only` | Measures CLI loading time and file sizes. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-readonly` | Tier 0 — read-only inspection | `read_only_runtime_candidate` | Inspects system files and configurations without execution. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-readonly policy` | Tier 1 — artifact-only planning/validation | `validation_only` | Generates read policy schema. | Writes policy JSON. | `none` | None. | No | No | No | Yes | No |
| `builder-readonly read` | Tier 1 — artifact-only planning/validation | `read_only_runtime_candidate` | Reads system files according to policy. | Writes read receipt JSON. | `none` | None. | No | No | No | Yes | No |
| `builder-readonly validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates read receipt JSON. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-verification` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates verification profile schemas. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-verify plan` | Tier 1 — artifact-only planning/validation | `artifact_only` | Generates a passive verification execution plan artifact only; no runtime start, shell execution, subprocess execution, model execution, MCP/tool invocation, Goose, deepagents, git mutation, or B2 patch authority. | Writes only the explicit verification execution plan JSON artifact requested by --output. | `none` | None. This is planned-only metadata and cannot authorize execution. | No | No | No | Yes | No |
| `builder-verify validate-plan` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a verification execution plan artifact without runtime start, shell execution, subprocess execution, model execution, MCP/tool invocation, Goose, deepagents, git mutation, or B2 patch authority. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-platform operator-status` | Tier 1 — artifact-only planning/validation | `artifact_only` | Generates operator status report derived from truth matrix and command authority; no execution. | Writes status JSON artifact if output path requested. | `none` | None. | No | No | No | Yes | No |
| `builder-platform next` | Tier 1 — artifact-only planning/validation | `artifact_only` | Generates operator next action report derived from truth matrix and evidence gaps; no execution. | Writes next action JSON artifact if output path requested. | `none` | None. | No | No | No | Yes | No |
| `builder-platform golden-path` | Tier 1 — artifact-only planning/validation | `artifact_only` | Generates operator golden path report derived from status and next primitive; no execution. | Writes golden path JSON artifact if output path requested. | `none` | None. | No | No | No | Yes | No |
| `builder-platform validate-golden-path` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates operator golden path report; no execution. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-platform demo-loop` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Runs a guided governed demo against a temporary detached worktree of an operator-designated target repo (AssetOverflow/core remains a supported profile). It never commits, pushes, starts Goose, calls models, invokes MCP, or mutates the source checkout. | Writes demo evidence artifacts to --output-dir and may apply/rollback one approved temporary documentation marker patch inside the demo worktree only. | `explicit_operator_invocation` | Operator must explicitly pass --approve before the temporary demo worktree patch is applied. | No | No | Yes | Yes | No |
| `builder-platform validate-demo-loop` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a governed demo loop report artifact without runtime start, model execution, source writes, or git mutation. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-platform wow` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Alias for the governed demo loop used for recording. It operates on a temporary detached worktree of the target repo only. | Writes demo evidence artifacts to --output-dir and may apply/rollback one approved temporary documentation marker patch inside the demo worktree only. | `explicit_operator_invocation` | Operator must explicitly pass --approve before the temporary demo worktree patch is applied. | No | No | Yes | Yes | No |
| `builder-verify approve-plan` | Tier 1 — artifact-only planning/validation | `artifact_only` | Generates a digest-bound HITL approval artifact for a passive verification execution plan only; no runtime start, direct execution, shell execution, subprocess execution, model execution, MCP/tool invocation, Goose, deepagents, git mutation, or B2 patch authority. | Writes only the explicit verification execution approval JSON artifact requested by --output. | `none` | None. The artifact binds human approval to a plan digest only and does not authorize execution. | No | No | No | Yes | No |
| `builder-verify validate-approval` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a verification execution approval artifact against its referenced passive plan without runtime start, direct execution, shell execution, subprocess execution, model execution, MCP/tool invocation, Goose, deepagents, git mutation, or B2 patch authority. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-verify validate-receipt` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a passive B1.3A verification execution receipt artifact against its referenced plan and approval without runtime start, direct execution, shell execution, subprocess execution, model execution, MCP/tool invocation, Goose, deepagents, git mutation, or B2 patch authority. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-verify evaluate-promotion` | Tier 1 — artifact-only planning/validation | `validation_only` | B2.0 machine-checkable promotion-gate evaluator: reads plan/approval/receipt (and optional ledger) artifacts, recomputes digest bindings and gate checks, and emits a passive promotion-evidence artifact. No subprocess, shell, model, MCP, Goose, deepagents, source writes, or matrix flips. | Writes only an optional explicit promotion-evidence JSON path when --output is provided. | `none` | None. Evidence of gate PASS is not operator approval and does not flip capability state. | No | No | No | Yes | No |
| `builder-verify validate-promotion-evidence` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a builder_ii.verification_promotion_evidence artifact for schema, gate shape, and non-authority invariants. No execution. | No writes. | `none` | None. | No | No | No | No | No |
| `builder-verify run-approved` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Runs exactly one bounded verification profile from fixed in-code argv with subprocess shell=False, after validating the plan artifact, approval artifact, and approval-to-plan binding. Supported profiles are the safe builder-II-argv profiles platform_status and docs_audit, plus the target-code-executing profiles pytest_full and builder_full, which run the target repository's own test/conftest/plugin code and therefore require an execution-risk acknowledgment on the approval before the runner will spawn them. The per-profile timeout is read from the approved plan (range-checked to [1,1800]s). This bounds invocation only, never invoked-code behavior; it is not a sandbox. | Writes only the explicit verification execution receipt artifact requested by --output, and only under the configured artifact root when output is inside the target repository. A target-code profile may leave incidental pytest cache/bytecode byproducts in the target repo; these are recorded as observed byproducts and any non-ignored change invalidates the receipt. | `hitl_artifact_required` | Requires a digest-bound verification execution approval artifact for the requested command profile and step id; a target-code profile additionally requires the approval to carry an execution-risk acknowledgment. | No | No | No | Yes | No |
| `builder-hitl` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Delegates execution request and receipt operations to subcommands. | No direct write authority at root CLI level. | `hitl_artifact_required` | Operator must sign hitl request and verify receipts. | No | No | No | No | No |
| `builder-orchestration` | Tier 1 — artifact-only planning/validation | `artifact_only` | Delegates passive plan setup, assignment rendering, validation, and dry-run subcommands. | No direct write authority at root CLI level. | `none` | None. | No | No | No | No | No |
| `builder-profile-pack` | Tier 1 — artifact-only planning/validation | `artifact_only` | Delegates passive profile-pack scaffold, render, dry-run, and validation subcommands. | No direct write authority at root CLI level. | `none` | None. | No | No | No | No | No |
| `builder-model-policy` | Tier 1 — artifact-only planning/validation | `artifact_only` | Delegates passive model client registry and routing policy subcommands. | No direct write authority at root CLI level. | `none` | None. | No | No | No | No | No |
| `builder-model` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Delegates governed model execution subcommands. | No direct write authority at root CLI level. | `none` | Delegated to subcommands. | No | No | No | No | No |
| `builder-platform` | Tier 1 — artifact-only planning/validation | `validation_only` | Delegates passive completion-truth matrix, status, and docs-audit rendering; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-memory` | Tier 1 — artifact-only planning/validation | `artifact_only` | Delegates governed memory atom, index, search, reconstruction, and validation subcommands without runtime execution, model calls, shell execution, hidden retrieval, or vector-store behavior. | No direct write authority at root CLI level; subcommands may write only explicit JSON memory artifacts when --output is supplied. | `none` | None. | No | No | No | No | No |
| `builder-config` | Tier 1 — artifact-only planning/validation | `artifact_only` | Delegates passive config schema rendering, source resolution, and validation; no runtime, model, shell, Goose, deepagents, MCP, tool, or patch execution. | No direct write authority at root CLI level; subcommands may write explicit JSON artifacts only when --output is supplied. | `none` | None. | No | No | No | No | No |
| `builder-setup` | Tier 1 — artifact-only planning/validation | `artifact_only` | Delegates passive setup plan, overlay plan, rollback snapshot, and validation subcommands; no setup apply, rollback execution, Goose start, model call, shell execution, deepagents runtime, MCP/tool invocation, or patch authority. | No direct write authority at root CLI level; artifact commands write only explicit JSON output artifacts when --output is supplied. | `none` | None. | No | No | No | No | No |
| `builder-workflow` | Tier 1 — artifact-only planning/validation | `artifact_only` | Delegates passive workflow state-machine subcommands; no runtime, model, shell, Goose, deepagents, or MCP execution. | Writes governed workflow artifacts only under explicit workflow output directories. | `none` | None. | No | No | No | Yes | No |
| `builder-ledger` | Tier 1 — artifact-only planning/validation | `artifact_only` | Delegates event-ledger list, replay, audit, export, and verification execution ledger query/validation/reconstruction subcommands without executing runtime work. | Writes replay and ledger export artifacts only when requested by ledger subcommands. | `none` | None. | No | No | No | Yes | No |
| `builder-session prepare-package` | Tier 1 — artifact-only planning/validation | `artifact_only` | Prepares context packaging and checks files. | Writes prepared package files locally. | `none` | None. | No | No | No | Yes | No |
| `builder-session validate-prepare-package` | Tier 1 — artifact-only planning/validation | `validation_only` | Performs validations on the prepared package directory structure. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-session summarize-prepare-package` | Tier 1 — artifact-only planning/validation | `validation_only` | Analyzes prepared package and generates a passive summary. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-context pack` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Invokes legacy external scanner or git commands. | Writes context bundle files. | `explicit_operator_invocation` | Explicit operator invocation only; no artifact approval chain. | No | No | No | Yes | No |
| `builder-context changed` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Queries git status or git diff via subprocess. | No changes to workspace. | `explicit_operator_invocation` | Explicit operator invocation only; no artifact approval chain. | No | No | No | No | No |
| `builder-context artifact` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Processes codebase scanning, potentially using external tools like repomix. | Creates context artifact files. | `explicit_operator_invocation` | Explicit operator invocation only; no artifact approval chain. | No | No | No | Yes | No |
| `builder setup` | Tier 1 — artifact-only planning/validation | `validation_only` | Fail-closed compatibility wrapper that redirects operators to governed builder-setup plan/overlay/rollback/apply commands; no setup apply is performed by this legacy surface. | No changes to workspace, target repo, Goose config, .goosehints, skills, recipes, or runtime state. | `none` | None. The command is informational only and exits non-zero after printing the governed migration path. | No | No | No | No | No |
| `builder onboarding` | Tier 1 — artifact-only planning/validation | `artifact_only` | Interactive guided onboarding flow delegating directly to governed builder-setup wizard; no Goose, model, MCP, deepagents, patch, or shell execution. | Writes passive onboarding/setup artifacts under explicit output directory only; no setup mutation or receipt generation. | `none` | None. | No | No | No | Yes | No |
| `builder init` | Tier 1 — artifact-only planning/validation | `artifact_only` | Unified onboarding orchestrator composing the governed onboarding pipeline (setup plan, overlay plan, rollback snapshot, intent report); prompts only for the four wizard decisions, registry-validates every answer, and echoes the five documented-default decisions; no Goose, model, MCP, deepagents, patch, or shell execution. | Writes passive onboarding/setup artifacts under the selected output directory only; never applies setup and never writes a receipt. | `none` | None. Prints the follow-up builder-setup apply command without an inline digest; apply approval happens only in the separately invoked apply step. | No | No | No | Yes | No |
| `builder pull` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Runs model-cache download helpers for explicitly selected local model tiers. | Writes model cache files outside the target repository through operator-invoked helper scripts. | `explicit_operator_invocation` | Explicit operator invocation only; no autonomous download or runtime authority. | No | No | No | No | Yes |
| `builder start` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Starts background runtime processes and servers. | Creates runtime process state and session context artifacts only; does not perform legacy setup writes. | `explicit_operator_invocation` | Explicit operator invocation only; no artifact approval chain. | No | No | No | No | Yes |
| `builder ask` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Queries model provider or MLX local runtime using user input. | Writes conversation history files locally. | `explicit_operator_invocation` | Explicit operator invocation only; no artifact approval chain. | No | No | No | Yes | No |
| `builder verify` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Invokes local pytest/runner test suites via subprocess. | No source code changes; generates test result files. | `explicit_operator_invocation` | Explicit operator invocation only; no artifact approval chain. | No | No | No | No | No |
| `builder benchmark` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Runs bounded local benchmark probes and may call the configured chat-completions endpoint if reachable. | Writes a benchmark report only when an explicit output path is provided. | `explicit_operator_invocation` | Explicit operator invocation only; no autonomous benchmark or model probing. | No | No | No | Yes | No |
| `builder capabilities` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Checks served-model capability gates and optionally runs a live chat smoke when --chat is passed. | No changes to workspace, target repository, or runtime state. | `explicit_operator_invocation` | Explicit operator invocation only; --chat is a live model probe. | No | No | No | No | No |
| `builder switch-model` | Tier 1 — artifact-only planning/validation | `validation_only` | Normalizes model aliases and prints environment lines only. | No changes to workspace, environment files, runtime state, or target repository. | `none` | None. | No | No | No | No | No |
| `builder models` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Inspects local model cache directories with bounded read-only filesystem and size probes. | No changes to workspace, cache contents, or target repository. | `explicit_operator_invocation` | Explicit operator invocation only. | No | No | No | No | No |
| `builder doctor` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Runs local readiness probes across repo path, Goose availability, backend health, recipes, compliance, and model cache state. | No changes to workspace, runtime state, or target repository. | `explicit_operator_invocation` | Explicit operator invocation only. | No | No | No | No | No |
| `builder status` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Reads backend health, Goose status, recipe validation, compliance, and local model cache status. | No changes to workspace, runtime state, or target repository. | `explicit_operator_invocation` | Explicit operator invocation only. | No | No | No | No | No |
| `builder config` | Tier 1 — artifact-only planning/validation | `validation_only` | Renders passive configuration and legacy setup redirect metadata. | No changes to workspace, runtime state, or target repository. | `none` | None. | No | No | No | No | No |
| `builder init-prompt` | Tier 1 — artifact-only planning/validation | `validation_only` | Renders the governed initialization prompt text. | No changes to workspace, runtime state, or target repository. | `none` | None. | No | No | No | No | No |
| `builder-goose manifest` | Tier 1 — artifact-only planning/validation | `validation_only` | Inspects Goose configuration manifest templates. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-goose validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Performs validations on active Goose session configs. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-goose readonly-audit` | Tier 1 — artifact-only planning/validation | `validation_only` | Ensures a target Goose session uses read-only tools only. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-goose validate-audit` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates the output of a Goose audit run. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-goose inspect-readonly` | Tier 1 — artifact-only planning/validation | `validation_only` | Performs read-only inspection validation on explicitly requested paths. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-goose validate-inspection` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates inspection config details. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-goose start-readonly` | Tier 3 — HITL-gated execution candidate | `read_only_runtime_candidate` | Starts a Goose session bounded by read-only policies. | No writes permitted. Strict environment isolation. | `explicit_operator_invocation` | Requires implicit or explicit HITL approval for launch. | No | No | No | No | No |
| `builder-goose close-readonly` | Tier 3 — HITL-gated execution candidate | `read_only_runtime_candidate` | Closes a governed Goose session and verifies no mutation occurred. | No writes permitted. Target filesystem is inspected for drift. | `explicit_operator_invocation` | Delegated to CLI lifecycle. | No | No | No | No | No |
| `builder-deepagents policy` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders governed deepagents policy metadata statically. | Writes policy JSON only to explicit artifact output paths. | `none` | None. | No | No | No | Yes | No |
| `builder-deepagents validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates deepagents spec metadata. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-deepagents readiness` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders optional deepagents dependency-readiness metadata. | Writes readiness JSON only to explicit artifact output paths. | `none` | None. | No | No | No | Yes | No |
| `builder-deepagents validate-readiness` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates deepagents dependency-readiness metadata. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-deepagents forge` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates or previews governed deepagent profile artifacts only; no native deepagents construction, model execution, shell execution, MCP/tool invocation, Goose activation, source writes outside bounded Forge artifacts, git mutation, or runtime promotion. | Dry-run writes nothing. Real emission writes only profiles/deepagents/{slug}.yaml and an optional profiles/deepagents/forge_{slug}.handoff.json handoff artifact. | `explicit_operator_invocation` | Operator must explicitly invoke Forge; emitted artifacts are not runtime approval or promotion authority. | No | No | No | Yes | No |
| `builder-deepagents delegate` | Tier 4 — forbidden/unpromoted automation | `forbidden_unpromoted` | Attempts to execute autonomous models. Forbidden. | No changes to workspace. | `forbidden_unpromoted` | Forbidden; no supported approval path. | No | No | No | No | No |
| `builder-deepagents work-plan` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive deepagents work plan statically. | Writes work plan JSON only to explicit artifact output paths. | `none` | None. | No | No | No | Yes | No |
| `builder-deepagents assign-subagent` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive subagent assignment statically. | Writes subagent assignment JSON only to explicit artifact output paths. | `none` | None. | No | No | No | Yes | No |
| `builder-deepagents record-result` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive subagent result statically. | Writes subagent result JSON only to explicit artifact output paths. | `none` | None. | No | No | No | Yes | No |
| `builder-deepagents review-result` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive subagent review statically. | Writes subagent review JSON only to explicit artifact output paths. | `none` | None. | No | No | No | Yes | No |
| `builder-deepagents request-human-gate` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive human gate request statically. | Writes human gate request JSON only to explicit artifact output paths. | `none` | None. | No | No | No | Yes | No |
| `builder-deepagents record-blocked-action` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive blocked action record statically. | Writes blocked action record JSON only to explicit artifact output paths. | `none` | None. | No | No | No | Yes | No |
| `builder-deepagents proposal-result` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive proposal result statically. | Writes proposal result JSON only to explicit artifact output paths. | `none` | None. | No | No | No | Yes | No |
| `builder-deepagents validate-work-artifact` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates passive deepagents work artifact metadata. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-deepagents backend-readiness` | Tier 1 — artifact-only planning/validation | `artifact_only` | Inspects the optional_deepagents protocol adapter exports and records a promotion-readiness gate without constructing an agent. | Writes backend readiness gate JSON only to explicit artifact output paths. | `explicit_operator_invocation` | Operator must explicitly assert AGENTS.md capability gates before the gate can pass. | No | No | No | Yes | No |
| `builder-deepagents execution-candidate` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a bounded deepagents protocol execution candidate without running a backend; optional_deepagents requires a passing backend-readiness gate. | Writes candidate JSON only to explicit artifact output paths. | `none` | None; later run-approved requires digest-bound HITL approval. | No | No | No | Yes | No |
| `builder-deepagents approve-candidate` | Tier 1 — artifact-only planning/validation | `artifact_only` | Binds human approval to the exact candidate digest via required --approval-actor/--approval-reason flags (non-interactive; no typed-prefix prompt); the digest-bound approval artifact seals the whole envelope (lane policy, root budget, allowed obligation kinds, refused lanes) when the candidate declares one. | Writes approval JSON only to explicit artifact output paths. | `explicit_operator_invocation` | Human actor and reason are required inputs; --native-backend-acknowledged is the second key required to seal an optional_deepagents obligation-bearing candidate. | No | No | No | Yes | No |
| `builder-deepagents run-approved` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Runs the approved protocol backend lane only after candidate and approval digests bind. With --obligation, each subagent runs its OWN mint-checked obligation task and each discharge is classified (CONTRACT_SATISFIED / DISCHARGED_UNVERIFIED / CONTRACT_VIOLATED / BLOCKED). | Writes run envelope, hash-chained events (including obligation_minted / obligation_mint_refused / obligation_consumed), replay report, ledger, receipt, and optional checkpoint under the approved output root. | `hitl_artifact_required` | Requires a valid builder_ii.deepagents_execution_approval bound to the exact candidate; an obligation-bearing run additionally requires a Ladder 4 seal and, for optional_deepagents, the two-key native acknowledgement. | No | No | No | Yes | No |
| `builder-deepagents replay-run` | Tier 1 — artifact-only planning/validation | `validation_only` | Reconstructs deepagents run state from event records only. | Writes replay report JSON only to explicit artifact output paths. | `none` | None; replay never reruns backend/model/tool work. | No | No | No | Yes | No |
| `builder-deepagents evidence-bundle` | Tier 1 — artifact-only planning/validation | `artifact_only` | Bundles deepagents candidate, approval, run, receipt, ledger, and replay evidence. | Writes evidence bundle JSON only to explicit artifact output paths. | `none` | None; evidence bundle does not grant authority. | No | No | No | Yes | No |
| `builder-deepagents resume-approved` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Resumes a checkpointed protocol backend run only when candidate and approval still bind exactly. | Appends hash-chained events and rewrites run envelope, replay, ledger, and receipt under the approved output root. | `hitl_artifact_required` | Requires the original approval and checkpoint bound to the same candidate. | No | No | No | Yes | No |
| `builder-deepagents run-plan` | Tier 1 — artifact-only planning/validation | `artifact_only` | Synthesizes legacy passive planning artifacts; not the promoted approved lane. | Writes synthetic runtime envelope and subagent receipts only to explicit artifact paths. | `explicit_operator_invocation` | Operator explicitly invokes the compatibility command; no approval artifact is consumed. | No | No | No | Yes | No |
| `builder-deepagents collect-results` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Collects subagent results into a final proposal result. | Writes proposal result JSON only to explicit artifact output paths. | `explicit_operator_invocation` | Delegated to CLI invocation. | No | No | No | Yes | No |
| `builder-hitl propose-patch` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a design-only patch proposal artifact. | Writes patch proposal JSON file to specified output. | `none` | None. | No | No | No | Yes | No |
| `builder-hitl approve-patch` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders a proposal's diff and patch digest, then mints a digest-bound approval artifact after an interactive digest-prefix confirmation. No patch is applied and no source is written. | Writes an approval JSON file to the specified output path only. | `none` | The operator must transcribe the patch-digest prefix at an interactive prompt; there is no non-interactive approval mode. | No | No | No | Yes | No |
| `builder-hitl apply-patch` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Applies a patch specifically from an approved proposal to a clean repository after rollback plan creation. | Modifies source tree according to the diff. Writes postflight and receipt records. | `hitl_artifact_required` | Requires a schema-valid, unexpired hitl_patch_approval artifact bound to the proposal's content and patch digests, verified inside apply_hitl_patch. Digest-chained binding, not a cryptographic signature. | No | No | Yes | Yes | No |
| `builder-hitl approve-rollback` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders a rollback plan's summary and digest, then mints a plan-bound rollback approval artifact after an interactive digest-prefix confirmation. No rollback is executed and no source is written. | Writes a rollback approval JSON file to the specified output path only. | `none` | The operator must transcribe the rollback-plan-digest prefix at an interactive prompt; there is no non-interactive approval mode. | No | No | No | Yes | No |
| `builder-hitl rollback` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Restores codebase to pre-apply state using the generated reverse patch, after a working-tree drift preflight. | Modifies source tree according to reverse diff. | `hitl_artifact_required` | Requires a schema-valid, unexpired hitl_rollback_approval artifact bound to the rollback plan's content and patch digests (not merely the machine-generated rollback plan), verified inside rollback_hitl_patch. | No | No | Yes | No | No |
| `builder-hitl request` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Collects and validates HITL request details. | Writes HITL request JSON artifact. | `hitl_artifact_required` | Requires explicit operator approval signature. | No | No | No | Yes | No |
| `builder-hitl receipt` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Records execution completion or failure receipt metadata. | Writes HITL receipt JSON artifact. | `hitl_artifact_required` | Operator must sign hitl request and verify receipts. | No | No | No | Yes | No |
| `builder-hitl validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates request and receipt artifact files against schema. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-hitl promotion-request` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive promotion request statically. | Writes promotion request JSON only to explicit artifact output paths. | `none` | None. | No | No | No | Yes | No |
| `builder-hitl promotion-review` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive promotion review statically. | Writes promotion review JSON only to explicit artifact output paths. | `none` | None. | No | No | No | Yes | No |
| `builder-hitl promotion-decision` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive promotion decision statically. | Writes promotion decision JSON only to explicit artifact output paths. | `none` | None. | No | No | No | Yes | No |
| `builder-hitl approval-boundary` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive approval boundary statically. | Writes approval boundary JSON only to explicit artifact output paths. | `none` | None. | No | No | No | Yes | No |
| `builder-hitl rejection-record` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive rejection record statically. | Writes rejection record JSON only to explicit artifact output paths. | `none` | None. | No | No | No | Yes | No |
| `builder-hitl validate-promotion` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates passive promotion bridge artifact files against schema. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-hitl candidate-manifest` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive execution candidate manifest statically. | Writes candidate manifest JSON only to explicit artifact output paths. | `none` | None. | No | No | No | Yes | No |
| `builder-hitl validate-candidate-manifest` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates passive candidate manifest and validation report files against schema. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-orchestration plan` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates plan structure statically without launching active agents. | Writes plan JSON file. | `none` | None. | No | No | No | Yes | No |
| `builder-orchestration render-assignment` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive Goal 2 assignment and orchestration plans from existing source artifacts and SHA-256 refs. | Writes assignment/orchestration JSON only to explicit artifact output paths. | `none` | None. | No | No | No | Yes | No |
| `builder-orchestration validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates v1 orchestration and Goal 2 assignment/orchestration artifacts. | Writes validation-report JSON only when an explicit output path is supplied. | `none` | None. | No | No | No | Yes | No |
| `builder-orchestration dry-run` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a passive dry-run explaining Goal 2 planned bindings without executing anything. | Writes dry-run JSON only to an explicit artifact output path. | `none` | None. | No | No | No | Yes | No |
| `builder-orchestration lane-policy` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders the Ladder 4 obligation lane policy from a fixed in-code table and checks each command-form discharge mechanism against COMMAND_AUTHORITY_REGISTRY (read-only). | Writes lane policy JSON only to an explicit artifact output path. | `none` | None. | No | No | No | Yes | No |
| `builder-orchestration validate-lane-policy` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates an orchestration lane policy artifact file (schema, totality, digest) and re-checks command-form discharge mechanisms against COMMAND_AUTHORITY_REGISTRY (read-only). | No writes; reads and validates an existing artifact file only. | `none` | None. | No | No | No | No | No |
| `builder-orchestration mint-obligation` | Tier 1 — artifact-only planning/validation | `artifact_only` | Builds a governed obligation artifact statically: derives and checks the lane against a supplied lane policy, pins its digest, and validates the result. Does not run, spawn, or enforce a runtime budget envelope. | Writes obligation JSON only to an explicit artifact output path. | `none` | None. The obligation is inert until a separately sealed runner consumes it (Ladder 4 PR-4). | No | No | No | Yes | No |
| `builder-orchestration validate-obligation` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates an orchestration obligation artifact file: schema, anti-dump, parent_ref XOR, budget shape, and digest re-derivation (tamper detection). | No writes; reads and validates an existing artifact file only. | `none` | None. | No | No | No | No | No |
| `builder-orchestration status` | Tier 1 — artifact-only planning/validation | `validation_only` | Deterministic read-only walk over a `builder-deepagents run-approved --obligation` output directory: re-derives the event chain fresh from the per-event files (tamper-sensitive) and renders one row per obligation with its board state (OPEN / SATISFIED / UNVERIFIED / VIOLATED / BLOCKED) and granted budget partition. No model, no execution. | Writes the rendered status-board JSON only to an explicit --output artifact path; never writes to the run directory it inspects. | `none` | None. | No | No | No | Yes | No |
| `builder-orchestration why` | Tier 1 — artifact-only planning/validation | `validation_only` | Deterministic read-only belief trace for one obligation, located from one of its lifecycle event files: re-walks the whole run fresh from disk and reports believed?/required evidence/attached/consumed. Only a CONTRACT_SATISFIED discharge over an intact chain is believed. No model, no execution. | Writes the belief-trace JSON only to an explicit --output artifact path; never writes to the run directory it inspects. | `none` | None. | No | No | No | Yes | No |
| `builder-profile-pack scaffold` | Tier 1 — artifact-only planning/validation | `artifact_only` | Scaffolds passive profile-pack manifests from local source refs and hashes only. | Writes profile-pack manifest JSON only to an explicit artifact output path. | `none` | None. | No | No | No | Yes | No |
| `builder-profile-pack render` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders a passive profile-pack render plan without materializing runtime behavior. | Writes render-plan JSON only to an explicit artifact output path. | `none` | None. | No | No | No | Yes | No |
| `builder-profile-pack validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates profile-pack lifecycle artifacts and can emit a passive validation report. | Writes validation-report JSON only when an explicit output path is supplied. | `none` | None. | No | No | No | Yes | No |
| `builder-profile-pack dry-run` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a passive dry-run artifact showing what would render without executing anything. | Writes dry-run JSON only to an explicit artifact output path. | `none` | None. | No | No | No | Yes | No |
| `builder-model-policy validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates passive model client registry, routing policy, and recommendation artifacts. | Writes validation report JSON only when an explicit output path is supplied. | `none` | None. | No | No | No | Yes | No |
| `builder-model-policy render` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive model routing recommendation artifacts from registry and policy metadata. | Writes recommendation JSON only to an explicit artifact output path. | `none` | None. | No | No | No | Yes | No |
| `builder-model-policy dry-run` | Tier 1 — artifact-only planning/validation | `artifact_only` | Performs a passive routing recommendation dry-run showing recommended models without executing anything. | Writes dry-run recommendation JSON only to an explicit artifact output path. | `none` | None. | No | No | No | Yes | No |
| `builder-model call` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Executes a governed model call using an enabled model client. | Writes model call envelope and receipt JSON to output paths. | `explicit_operator_invocation` | Delegated to CLI invocation. | No | No | No | Yes | No |
| `builder-model validate-receipt` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a model call receipt artifact against its schema. | No target repo writes. | `none` | None. | No | No | No | No | No |
| `builder-model standalone-call` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Executes a governed model call without logging to the workflow ledger. Intended for testing and offline evaluation only; no session state is produced. | Writes model call envelope and receipt JSON to explicit output paths only. | `explicit_operator_invocation` | Delegated to CLI invocation. No ledger event is recorded; operator is responsible for audit trail outside of session context. | No | No | No | Yes | No |
| `builder-platform matrix` | Tier 1 — artifact-only planning/validation | `validation_only` | Renders the static source-derived completion truth matrix as JSON after validating shape and authority references. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-platform status` | Tier 1 — artifact-only planning/validation | `validation_only` | Summarizes the completion truth matrix without inspecting runtime systems or target repositories. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-platform known-limitations` | Tier 1 — artifact-only planning/validation | `validation_only` | Renders the known-limitations document from the completion truth matrix after validating matrix shape; no verification, model, tool, Goose, deepagents, shell, or MCP behavior. | Writes only the explicit --output document path when provided; stdout rendering leaves the workspace untouched. | `none` | None. | No | No | No | Yes | No |
| `builder-platform audit-docs` | Tier 1 — artifact-only planning/validation | `validation_only` | Scans README.md and docs/**/*.md for false operational-completion claims without invoking external tools. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-platform r1-closure` | Tier 1 — artifact-only planning/validation | `artifact_only` | Generates config schema, resolution, setup plan, overlay plan, rollback snapshot, onboarding intent, and R1 closure report artifacts in the requested output directory without runtime execution. | Writes only explicit R1 closure and proof artifacts into the provided output directory. No target repository or source files are modified. | `none` | None. Generating closure and onboarding proof artifacts never applies mutation. | No | No | No | Yes | No |
| `builder-platform validate-r1-closure` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates an R1 closure report artifact and its referenced R1 chain evidence files without executing runtime or model operations. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-memory atom` | Tier 1 — artifact-only planning/validation | `artifact_only` | Wraps one explicit validated source artifact as a governed memory atom without runtime execution, model authority, shell execution, hidden memory, or vector-store retrieval. | Writes one memory atom JSON artifact only to the explicit output path when --output is supplied. | `none` | None. | No | No | No | Yes | No |
| `builder-memory index` | Tier 1 — artifact-only planning/validation | `artifact_only` | Builds a deterministic memory index from explicit memory atom artifacts only; no runtime execution, model calls, shell execution, hidden retrieval, or vector-store behavior. | Writes one memory index JSON artifact only to the explicit output path when --output is supplied. | `none` | None. | No | No | No | Yes | No |
| `builder-memory search` | Tier 1 — artifact-only planning/validation | `artifact_only` | Searches an explicit memory index with deterministic lexical scoring only; no model calls, semantic retrieval, hidden memory, shell execution, or runtime behavior. | Writes one memory search result JSON artifact only to the explicit output path when --output is supplied. | `none` | None. | No | No | No | Yes | No |
| `builder-memory reconstruct` | Tier 1 — artifact-only planning/validation | `artifact_only` | Reconstructs reviewable context from an explicit memory index without runtime execution, model authority, shell execution, hidden retrieval, or target repo mutation. | Writes one memory reconstruction JSON artifact only to the explicit output path when --output is supplied. | `none` | None. | No | No | No | Yes | No |
| `builder-memory validate-atom` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a memory atom artifact without runtime execution, model calls, shell execution, or memory mutation. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-memory validate-index` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a memory index artifact without runtime execution, model calls, shell execution, or memory mutation. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-memory validate-reconstruction` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a memory reconstruction artifact without runtime execution, model calls, shell execution, or memory mutation. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-memory validate-search-result` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a memory search result artifact without runtime execution, model calls, shell execution, or memory mutation. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-config schema` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders the static generic-first config schema; no runtime, model, shell, Goose, deepagents, MCP, tool, or patch execution. | Writes schema JSON only to an explicit artifact output path when --output is supplied. | `none` | None. | No | No | No | Yes | No |
| `builder-config resolve` | Tier 1 — artifact-only planning/validation | `artifact_only` | Resolves passive config sources and validates path policy without running subprocesses or invoking runtime systems. | Writes resolution JSON only to an explicit artifact output path when --output is supplied. | `none` | None. | No | No | No | Yes | No |
| `builder-config validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates config schema or resolution artifacts without runtime, model, shell, Goose, deepagents, MCP, tool, or patch execution. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-setup plan` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a passive setup plan from resolved config; no setup apply, rollback, runtime start, model call, shell execution, Goose start, deepagents construction, MCP/tool invocation, or patch authority. | Writes setup plan JSON only to an explicit artifact output path when --output is supplied. | `none` | None. | No | No | No | Yes | No |
| `builder-setup validate-plan` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a passive setup plan artifact without applying setup, rollback, Goose, deepagents, model, MCP/tool, shell, or patch behavior. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-setup overlay-plan` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a passive setup overlay plan from a setup plan; runtime execution, model execution, shell execution, Goose runtime, deepagents runtime, MCP/tool invocation, patch authority, setup apply, and setup rollback execution are disabled. | Writes setup overlay JSON only to an explicit artifact output path when --output is supplied. | `none` | None. | No | No | No | Yes | No |
| `builder-setup validate-overlay-plan` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a passive setup overlay plan without setup apply, rollback execution, runtime start, model call, shell execution, Goose, deepagents, MCP/tool, or patch behavior. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-setup rollback-snapshot` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a passive setup rollback snapshot artifact by inspecting prior file states; runtime execution, model execution, shell execution, Goose runtime, deepagents runtime, MCP/tool invocation, patch authority, setup apply, and setup rollback execution are disabled. | Writes rollback snapshot JSON only to an explicit artifact output path when --output is supplied; it does not write target paths or secure prior-content stores. | `none` | None. | No | No | No | Yes | No |
| `builder-setup validate-rollback-snapshot` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a passive setup rollback snapshot without rollback execution, setup apply, runtime start, model call, shell execution, Goose, deepagents, MCP/tool, or patch behavior. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-setup apply` | Tier 2 — operator-managed setup/runtime helper | `enabled` | Digest-bound governed setup write only; runtime execution, model execution, shell/subprocess execution, Goose runtime, deepagents runtime, MCP/tool invocation, patch authority, rollback execution, and B1 verification execution are disabled. | Writes only declared setup target paths from a validated overlay plan plus the explicit setup receipt output; source writes are disabled except declared artifact-root setup metadata/config paths. | `explicit_operator_invocation` | Operator must approve the overlay_plan_digest before any mutation: --approve-digest matching overlay_plan_digest (scripted), or an interactive typed digest-prefix confirmation when the flag is omitted; --rollback-snapshot must match the overlay. The receipt records approval_mode (explicit_digest_bound_cli_flag or interactive_digest_prefix_confirmation). | No | No | Yes | Yes | No |
| `builder-setup validate-receipt` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates setup apply receipts without setup apply, rollback execution, runtime start, model call, shell execution, Goose, deepagents, MCP/tool, or patch behavior. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-setup rollback` | Tier 2 — operator-managed setup/runtime helper | `enabled` | Digest-bound governed setup rollback execution only; B1 verification execution, B2 patch rollback, generic repository rollback, git rollback, shell/subprocess, runtime execution, model/provider, MCP/tool, Goose runtime, deepagents runtime, patch authority, and autonomous rollback are disabled. | Touches only changed_paths recorded in an applied setup receipt and covered by the supplied rollback snapshot; writes a setup rollback receipt only to explicit --output. | `explicit_operator_invocation` | Operator must approve the setup receipt digest before any mutation: --approve-digest matching the receipt digest (scripted), or an interactive typed digest-prefix confirmation when the flag is omitted; the rollback snapshot must match receipt digests. The rollback receipt records approval_mode (explicit_digest_bound_cli_flag or interactive_digest_prefix_confirmation). | No | No | Yes | Yes | No |
| `builder-setup validate-rollback-receipt` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates setup rollback receipts without setup apply, rollback execution, runtime start, model call, shell execution, Goose, deepagents, MCP/tool, git, generic rollback, B1, B2, or patch behavior. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder-setup init` | Tier 1 — artifact-only planning/validation | `artifact_only` | Non-interactive governed onboarding wrapper generating passive setup plan, overlay plan, rollback snapshot, and intent report; no Goose, model, MCP, deepagents, patch, or shell execution. | Writes passive setup artifacts under explicit output directory only; no setup mutation or receipt generation. | `none` | None. | No | No | No | Yes | No |
| `builder-setup wizard` | Tier 1 — artifact-only planning/validation | `artifact_only` | Interactive governed onboarding wizard generating passive setup plan, overlay plan, rollback snapshot, and intent report; no Goose, model, MCP, deepagents, patch, or shell execution. | Writes passive setup artifacts under explicit output directory only; no setup mutation or receipt generation. | `none` | None. | No | No | No | Yes | No |
| `builder-setup validate-onboarding-intent` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates onboarding intent reports without setup apply, rollback execution, runtime start, model call, shell execution, Goose, deepagents, MCP/tool, git, or patch behavior. | No changes to workspace. | `none` | None. | No | No | No | No | No |
| `builder workflow plan` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a passive governed workflow plan; no model, shell, Goose, deepagents, MCP, or target-repo execution. | Writes workflow session, profile, routing, orchestration, work-plan, event, replay, ledger, and status artifacts under the workflow output directory. | `none` | None. | No | No | No | Yes | No |
| `builder workflow promote` | Tier 1 — artifact-only planning/validation | `artifact_only` | Records passive HITL promotion artifacts only; does not authorize execution. | Writes promotion request, review, decision, approval-boundary, transition, event, replay, ledger, and status artifacts. | `none` | None. | No | No | No | Yes | No |
| `builder workflow candidate` | Tier 1 — artifact-only planning/validation | `artifact_only` | Records a passive execution candidate manifest without invoking commands. | Writes candidate manifest, validation report, transition, event, replay, ledger, and status artifacts. | `none` | None. | No | No | No | Yes | No |
| `builder workflow verify-chain` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates artifact index and chain refs without running external tools. | Writes artifact index, chain verification, transition, event, replay, ledger, and status artifacts. | `none` | None. | No | No | No | Yes | No |
| `builder workflow handoff` | Tier 1 — artifact-only planning/validation | `artifact_only` | Writes passive handoff summaries after chain verification; no runtime activation. | Writes handoff note, golden-path summary, demo README, transition, event, replay, ledger, and status artifacts. | `none` | None. | No | No | No | Yes | No |
| `builder workflow status` | Tier 1 — artifact-only planning/validation | `validation_only` | Replays workflow events to reconstruct status without executing runtime work. | Writes refreshed replay, ledger, and status artifacts. | `none` | None. | No | No | No | Yes | No |
| `builder ledger list` | Tier 1 — artifact-only planning/validation | `validation_only` | Reads workflow event ledgers and reconstructs status summaries only. | Read-only ledger inspection; no workspace changes. | `none` | None. | No | No | No | No | No |
| `builder ledger replay` | Tier 1 — artifact-only planning/validation | `validation_only` | Replays event records deterministically without executing runtime work. | Writes replay report JSON to an explicit or default workflow artifact path. | `none` | None. | No | No | No | Yes | No |
| `builder ledger audit` | Tier 1 — artifact-only planning/validation | `validation_only` | Scans event subject refs for a requested artifact SHA; no execution. | Read-only ledger inspection; no workspace changes. | `none` | None. | No | No | No | No | No |
| `builder ledger query-receipts` | Tier 1 — artifact-only planning/validation | `validation_only` | Reads existing verification execution ledger records under .builder/ledger, validates records, and filters by receipt digest, chain digest, receipt status, or runner mode without replay, subprocess, shell, model, MCP, Goose, or deepagents runtime. | Read-only verification execution ledger inspection; no source, target repo, git, memory, patch, artifact, or B2 authority writes. | `none` | None. | No | No | No | No | No |
| `builder ledger validate-receipts` | Tier 1 — artifact-only planning/validation | `validation_only` | Reads existing verification execution ledger records under .builder/ledger and emits a deterministic integrity report without replay execution, subprocess, shell, model, MCP, Goose, or deepagents runtime. | Read-only verification execution ledger integrity validation; no source, target repo, git, memory, patch, artifact, or B2 authority writes. | `none` | None. | No | No | No | No | No |
| `builder ledger reconstruct-receipts` | Tier 1 — artifact-only planning/validation | `validation_only` | Reads existing verification execution ledger records under .builder/ledger and reconstructs passive receipt-chain projections without replay execution, subprocess, shell, model, MCP, Goose, or deepagents runtime. | Read-only verification execution ledger reconstruction; no source, target repo, git, memory, patch, artifact, or B2 authority writes. | `none` | None. | No | No | No | No | No |
| `builder ledger export` | Tier 1 — artifact-only planning/validation | `artifact_only` | Exports event ledger artifacts without runtime execution. | Writes event ledger export JSON to an explicit or default workflow artifact path. | `none` | None. | No | No | No | Yes | No |
| `builder ledger index-receipt` | Tier 1 — artifact-only planning/validation | `artifact_only` | Passively validates and indexes an existing B1.3 verification execution plan, approval, and receipt chain without subprocess, shell, model, MCP, Goose, deepagents, replay execution, patch behavior, git mutation, or source writes. | Writes one verification execution ledger record only under the target repo .builder/ledger directory. | `none` | None. | No | No | No | Yes | No |
| `builder-workflow plan` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a passive governed workflow plan; no model, shell, Goose, deepagents, MCP, or target-repo execution. | Writes workflow session, profile, routing, orchestration, work-plan, event, replay, ledger, and status artifacts under the workflow output directory. | `none` | None. | No | No | No | Yes | No |
| `builder-workflow promote` | Tier 1 — artifact-only planning/validation | `artifact_only` | Records passive HITL promotion artifacts only; does not authorize execution. | Writes promotion request, review, decision, approval-boundary, transition, event, replay, ledger, and status artifacts. | `none` | None. | No | No | No | Yes | No |
| `builder-workflow candidate` | Tier 1 — artifact-only planning/validation | `artifact_only` | Records a passive execution candidate manifest without invoking commands. | Writes candidate manifest, validation report, transition, event, replay, ledger, and status artifacts. | `none` | None. | No | No | No | Yes | No |
| `builder-workflow verify-chain` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates artifact index and chain refs without running external tools. | Writes artifact index, chain verification, transition, event, replay, ledger, and status artifacts. | `none` | None. | No | No | No | Yes | No |
| `builder-workflow handoff` | Tier 1 — artifact-only planning/validation | `artifact_only` | Writes passive handoff summaries after chain verification; no runtime activation. | Writes handoff note, golden-path summary, demo README, transition, event, replay, ledger, and status artifacts. | `none` | None. | No | No | No | Yes | No |
| `builder-workflow status` | Tier 1 — artifact-only planning/validation | `validation_only` | Replays workflow events to reconstruct status without executing runtime work. | Writes refreshed replay, ledger, and status artifacts. | `none` | None. | No | No | No | Yes | No |
| `builder-ledger list` | Tier 1 — artifact-only planning/validation | `validation_only` | Reads workflow event ledgers and reconstructs status summaries only. | Read-only ledger inspection; no workspace changes. | `none` | None. | No | No | No | No | No |
| `builder-ledger replay` | Tier 1 — artifact-only planning/validation | `validation_only` | Replays event records deterministically without executing runtime work. | Writes replay report JSON to an explicit or default workflow artifact path. | `none` | None. | No | No | No | Yes | No |
| `builder-ledger audit` | Tier 1 — artifact-only planning/validation | `validation_only` | Scans event subject refs for a requested artifact SHA; no execution. | Read-only ledger inspection; no workspace changes. | `none` | None. | No | No | No | No | No |
| `builder-ledger query-receipts` | Tier 1 — artifact-only planning/validation | `validation_only` | Reads existing verification execution ledger records under .builder/ledger, validates records, and filters by receipt digest, chain digest, receipt status, or runner mode without replay, subprocess, shell, model, MCP, Goose, or deepagents runtime. | Read-only verification execution ledger inspection; no source, target repo, git, memory, patch, artifact, or B2 authority writes. | `none` | None. | No | No | No | No | No |
| `builder-ledger validate-receipts` | Tier 1 — artifact-only planning/validation | `validation_only` | Reads existing verification execution ledger records under .builder/ledger and emits a deterministic integrity report without replay execution, subprocess, shell, model, MCP, Goose, or deepagents runtime. | Read-only verification execution ledger integrity validation; no source, target repo, git, memory, patch, artifact, or B2 authority writes. | `none` | None. | No | No | No | No | No |
| `builder-ledger reconstruct-receipts` | Tier 1 — artifact-only planning/validation | `validation_only` | Reads existing verification execution ledger records under .builder/ledger and reconstructs passive receipt-chain projections without replay execution, subprocess, shell, model, MCP, Goose, or deepagents runtime. | Read-only verification execution ledger reconstruction; no source, target repo, git, memory, patch, artifact, or B2 authority writes. | `none` | None. | No | No | No | No | No |
| `builder-ledger export` | Tier 1 — artifact-only planning/validation | `artifact_only` | Exports event ledger artifacts without runtime execution. | Writes event ledger export JSON to an explicit or default workflow artifact path. | `none` | None. | No | No | No | Yes | No |
| `builder-ledger index-receipt` | Tier 1 — artifact-only planning/validation | `artifact_only` | Passively validates and indexes an existing B1.3 verification execution plan, approval, and receipt chain without subprocess, shell, model, MCP, Goose, deepagents, replay execution, patch behavior, git mutation, or source writes. | Writes one verification execution ledger record only under the target repo .builder/ledger directory. | `none` | None. | No | No | No | Yes | No |
| `builder-mcp` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Delegates governed MCP policy and execution subcommands. | No direct write authority at root CLI level. | `none` | Delegated to subcommands. | No | No | No | No | No |
| `builder-mcp inventory` | Tier 1 — artifact-only planning/validation | `artifact_only` | Outputs static passive MCP inventory artifact. | Writes artifact to specified path if output flag passed. | `none` | None. | No | No | No | Yes | No |
| `builder-mcp policy` | Tier 1 — artifact-only planning/validation | `artifact_only` | Validates or emits passive MCP policy artifact. | Writes artifact to specified path if output flag passed. | `none` | None. | No | No | No | Yes | No |
| `builder-mcp call` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Executes explicitly bounded low-risk MCP stub call from envelope. | Writes receipt and operational ledger events. | `explicit_operator_invocation` | Explicit envelope passing. | No | No | No | Yes | Yes |
| `builder-mcp standalone-call` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Executes explicitly bounded low-risk MCP stub call from envelope without logging to the workflow ledger. | Writes receipt to explicit output path only. | `explicit_operator_invocation` | Explicit envelope passing. | No | No | No | Yes | No |
| `builder-tools invoke` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Executes explicitly bounded low-risk tool stub call from envelope. | Writes receipt and operational ledger events. | `explicit_operator_invocation` | Explicit envelope passing. | No | No | No | Yes | Yes |
| `builder-tools standalone-invoke` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Executes explicitly bounded low-risk tool stub call from envelope without logging to the workflow ledger. | Writes receipt to explicit output path only. | `explicit_operator_invocation` | Explicit envelope passing. | No | No | No | Yes | No |
