# Command Authority

Generated from `builder_ii.governance.authority`. Do not hand-edit: this file is hashed
into every governed workflow event as `policy_snapshot_ref`, and
`tests/test_command_authority.py` compares it byte for byte against its generator.

```
uv run python -m builder_ii.governance.authority > docs/COMMAND_AUTHORITY.md
```

## Assurance states

`Assurance` is authoritative for risk interpretation. It is derived from the record, never
declared by it, and `Assurance Derived From` names the single fact that decided it.

- `PASSIVE_ARTIFACT_VERIFIED` — Builds, validates, or reads governed artifacts and renders them. It starts no runtime, spawns no process, calls no provider, and writes nothing outside the artifact store.
- `LOCAL_STATE_MUTATION_VERIFIED` — Writes or deletes builder-II's own local state -- runtime markers, lockfiles, caches -- outside the artifact store and outside every target repository. It starts no runtime, spawns no process, and calls no provider. Nothing snapshots the write, so it is undone by re-establishing the state, never by a rollback.
- `READ_ONLY_RUNTIME_VERIFIED` — Starts, or hands the operator's terminal to, a runtime whose policy denies writes. The read-only boundary is enforced by that runtime's own preflight and postflight, never by the caller's intent.
- `BOUNDED_EXECUTION_VERIFIED` — Causes work to run -- a subprocess, an external tool, or a sealed backend -- inside a fixed, pre-approved envelope: fixed argv with shell=False or a digest-bound seal, an approval, and a digest-bound receipt. It attests the envelope of the invocation. It never attests the behaviour of the code that ran inside it.
- `MUTATION_WITH_ROLLBACK_VERIFIED` — Writes to the target repository's source tree or git state, and only behind an interactive digest-prefix approval, a required verification receipt, and a snapshot that makes the write reversible.
- `LIVE_PROVIDER_VERIFIED` — Reaches a live model provider over the network. Its output is not deterministic and is never, on its own, evidence.
- `DEMO_ONLY_VERIFIED` — Exercised end to end only inside the governed demo loop, against a synthetic target. A demo pass is not evidence for the corresponding real lane.
- `BLOCKED_BY_EVIDENCE` — No claim is supported: the capability is not operationally verified, or its command surface is a forbidden or unpromoted record. This is the state that absence takes. It is never a default for something that runs.
- `SAFETY_CRITICAL_PROHIBITED` — Names a capability whose promotion is refused regardless of the evidence offered for it. `allows_memory_mutation` is the only one, and `validate_registry_invariants` rejects it at every tier, so the state is derivable but no row or record carries it: a record claiming the flag is refused before anything can read its state. Unlike BLOCKED_BY_EVIDENCE, no evidence unblocks it.

## Capabilities

A record carries 11 capability flags. The `Capabilities` column names exactly the ones
it sets, so a row reading `—` claims none.

10 of the 11 raise the assurance state. Only `allows_artifact_writes` does not, and that
is correct rather than an oversight: `PASSIVE_ARTIFACT_VERIFIED` already permits writing
to the artifact store, so a command that writes only artifacts is passive by definition.

## Declared records (379)

Authority written down, command by command.

| Command Name | Tier | State | Runtime Boundary | Write Boundary | Approval Mode | Approval Boundary | Capabilities | Assurance | Assurance Derived From |
|---|---|---|---|---|---|---|---|---|---|
| `builder` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Delegates to helper subcommands; root CLI does not execute direct agent/model loops. | No direct write authority at root CLI level. | `explicit_operator_invocation` | Operator must explicitly run command options from active terminal. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder tui` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect hitl` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect profile` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect model` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect promote` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect postflight` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect goose` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect code-vault` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder hitl` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder profile` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder model` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder promote` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder postflight` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder goose` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder code-vault` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect hitl status` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect hitl chain` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect hitl pending` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect hitl approval` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect hitl evidence` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect hitl execution` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect hitl promote` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect hitl replay` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect profile status` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect profile lifecycle` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect profile validate` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect profile render-plan` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect profile dry-run` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect profile resolve` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect profile history` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect model routing show` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect model routing simulate` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect model routing candidates` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect model routing policy` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect model routing execution-policy` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect model routing validate` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect model registry show` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect model registry diff` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect promote status` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect promote readiness` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect promote artifact` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect promote decision` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect promote compatibility` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect promote history` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect promote gates` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect postflight status` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect postflight record` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect postflight verify` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect postflight governance` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect postflight actions` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect postflight refs` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect postflight validate` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect goose status` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect goose manifest` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect goose links` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect goose actions` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect goose governance` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect goose validate` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect goose approval` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect code-vault status` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect code-vault frame` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect code-vault determinism` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect code-vault recall` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect code-vault lint` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect code-vault context` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect code-vault governance` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect code-vault validate` | Tier 0 — read-only inspection | `spec_only` | Reads existing governed artifact files and renders terminal inspection output only; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace, artifact store, runtime state, target repository, git, or memory. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder stratum` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Reads passive governance artifacts and registries to render a spatial instrument console (chain spine, model/agent matrices, workflow lanes, operator next, platform audit); delegates execution to core tools. Every launch path (`builder stratum`, `builder-stratum`, `builder-platform tui`, `python -m builder_ii.tui`) calls enforce_command_authority against this record before constructing the app; none is exempt. Command tier evaluation is real: it asks check_command_authority rather than guessing from the tier. No chain digest is displayed, because verify_artifact_chain exposes none; the surface renders an explicit absence marker instead of a synthesized value. It starts exactly one runtime, and never itself: the Goose keybinding suspends the TUI and hands the operator's terminal to builder-goose start-readonly with a fixed argv and shell=False, so that command applies its own read-only policy, emits its own launch and close receipts, and runs its own no-mutation postflight. It fails closed twice before anything spawns -- enforce_command_authority must permit the governed command, and either a valid read_only manifest already exists or the operator confirms passive auto-prep (else the hand-off aborts). It never spawns Goose directly and never selects Goose builtins. HITL approve/reject never mutate approval state and are not pending features: a surface that renders a digest must not harvest its confirmation, so they refuse and name the governed CLI via the Command Composer. The HITL diff viewer renders the bound patch proposal's unified diff read-only and applies nothing. | Does not write target source, approval state, or HITL digests. The prepare-package and subagent-dispatch keybindings collect operator input and then refuse, naming builder-session prepare-package and builder-deepagents assign-subagent and prefilling the Command Composer. After an explicit ConfirmScreen yes on G only, stratum_prepare may create empty .builder scaffold dirs and one passive read_only Goose session manifest under .builder/goose (local scaffolding). Receipts under .builder/receipts are written by builder-goose start-readonly, never by the render surface. | `explicit_operator_invocation` | Operator must explicitly invoke one of STRATUM's launch commands from an active terminal; enforce_command_authority evaluates this record on every one of them before the app is constructed. No keybinding originates authority: every action that would write, approve, or dispatch is a constitutive refusal that names the governed command, except the operator-confirmed passive Goose manifest mint (local scaffolding only via stratum_prepare). The one runtime it starts is started by that governed command in the operator's own terminal, never by the TUI harvesting a confirmation for it. It executes nothing else and claims no execution: the command palette is a tier-permission inspector, and the CLI passthrough is a context-injecting composer that surfaces a command for the operator to run in their own terminal. | `runtime_start`, `artifact_writes`, `external_tool_invocation` | `READ_ONLY_RUNTIME_VERIFIED` | `allows_runtime_start` is set |
| `builder-stratum` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Console-script alias of `builder stratum`. Same instrument console, same Goose hand-off, same refuse-and-compose boundaries. | Matches `builder stratum`: passive operator-confirmed .builder Goose manifest via stratum_prepare only; target source and HITL digests stay off-limits. | `explicit_operator_invocation` | Explicit operator invocation of the console script. Authority gate matches `builder stratum`: enforce_command_authority against this same record. | `runtime_start`, `artifact_writes`, `external_tool_invocation` | `READ_ONLY_RUNTIME_VERIFIED` | `allows_runtime_start` is set |
| `builder-tui-inspection` | Tier 1 — artifact-only planning/validation | `smoke_only` | Launches passive TUI platform status panels; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-runtime` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Interacts with local server endpoints, background agents, and runtime indicators. | Writes session runtime lockfiles and state indicators locally. | `explicit_operator_invocation` | Operator must trigger control signals manually. | `runtime_start`, `state_writes`, `readonly_subprocess`, `external_tool_invocation` | `READ_ONLY_RUNTIME_VERIFIED` | `allows_runtime_start` is set |
| `builder-runtime status` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Inspects local server endpoints, marker files, and listener processes using read-only probes. | No changes to workspace, runtime marker, or target repository. | `explicit_operator_invocation` | Explicit operator invocation only; no autonomous runtime probing. | `readonly_subprocess`, `external_tool_invocation` | `BOUNDED_EXECUTION_VERIFIED` | `allows_external_tool_invocation` is set |
| `builder-runtime clear-marker` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Does not touch live processes; only clears the local runtime marker artifact. | Deletes or rewrites the builder runtime marker under configured local state paths. | `explicit_operator_invocation` | Explicit operator invocation only. | `state_writes` | `LOCAL_STATE_MUTATION_VERIFIED` | `allows_state_writes` is set |
| `builder-runtime stop` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Stops local runtime listener processes selected by the configured port and marker heuristics. | Clears local runtime marker unless --keep-marker is used; does not mutate source repositories. | `explicit_operator_invocation` | Explicit operator invocation only; foreign process termination requires literal confirmation. | `process_control`, `state_writes`, `readonly_subprocess` | `BOUNDED_EXECUTION_VERIFIED` | `allows_process_control` is set |
| `builder-runtime reset` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Composes stop plus marker cleanup for the local runtime listener. | Clears local runtime marker after attempted stop; does not mutate source repositories. | `explicit_operator_invocation` | Explicit operator invocation only; foreign process termination requires literal confirmation. | `process_control`, `state_writes`, `readonly_subprocess` | `BOUNDED_EXECUTION_VERIFIED` | `allows_process_control` is set |
| `builder-lanes` | Tier 1 — artifact-only planning/validation | `validation_only` | Evaluates passive rules and checklists; no subprocess execution. | No changes to workspace. | `none` | Passive check, no approval required. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-tools` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Delegates governed external tool execution subcommands. | No direct write authority at root CLI level. | `none` | Delegated to subcommands. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder inspect` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Delegates read-only inspection subcommands. | Read-only. No write authority. | `none` | Passive commands, no approval required. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-tools list` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Runs local PATH/version probes for known external developer tools. | No changes to workspace, target repository, or external tool state. | `explicit_operator_invocation` | Explicit operator invocation only. | `readonly_subprocess`, `external_tool_invocation` | `BOUNDED_EXECUTION_VERIFIED` | `allows_external_tool_invocation` is set |
| `builder-tools check` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Runs bounded local version probes for known external developer tools. | No changes to workspace, target repository, or external tool state. | `explicit_operator_invocation` | Explicit operator invocation only. | `readonly_subprocess`, `external_tool_invocation` | `BOUNDED_EXECUTION_VERIFIED` | `allows_external_tool_invocation` is set |
| `builder-tools missing` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Runs bounded local version probes to identify missing required tools. | No changes to workspace, target repository, or external tool state. | `explicit_operator_invocation` | Explicit operator invocation only. | `readonly_subprocess`, `external_tool_invocation` | `BOUNDED_EXECUTION_VERIFIED` | `allows_external_tool_invocation` is set |
| `builder-context` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Legacy context builder; delegates execution details to subcommands. | No direct write authority at root CLI level. | `explicit_operator_invocation` | Legacy context generator; operator must run command explicitly. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-git-state` | Tier 1 — artifact-only planning/validation | `artifact_only` | Executes local git queries via read-only subprocess. | Writes declarative state file artifacts in workspace. | `none` | Passive git branch check. | `artifact_writes`, `readonly_subprocess` | `BOUNDED_EXECUTION_VERIFIED` | `allows_readonly_subprocess` is set |
| `builder-targets` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Delegates target operations to helper subcommands; root CLI does not execute direct agent loops. | No direct write authority at root targets CLI level. | `explicit_operator_invocation` | Operator must explicitly run command options from active terminal. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-targets list` | Tier 0 — read-only inspection | `spec_only` | Retrieves target profile metadata list; no execution. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-targets show` | Tier 0 — read-only inspection | `spec_only` | Retrieves target profile details; no execution. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-targets validate` | Tier 0 — read-only inspection | `spec_only` | Validates target profile registry or artifact. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-targets artifact` | Tier 1 — artifact-only planning/validation | `spec_only` | Resolves target details without execution. | Writes target profile JSON artifact to path. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-targets demo` | Tier 0 — read-only inspection | `spec_only` | Retrieves target profile demo recipe; no execution. | No changes to workspace. | `none` | None. | — | `DEMO_ONLY_VERIFIED` | the command name says `demo` |
| `builder-targets doctor` | Tier 1 — artifact-only planning/validation | `validation_only` | V.4: doctor CORE target isolation (invariants/catalog/coupling). Does not run semgrep, mutate CORE repo, or claim Workbench identity. | Writes doctor report JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-targets readonly-founder-demo` | Tier 1 — artifact-only planning/validation | `spec_only` | Generates passive read-only founder demo planning artifacts; no execution. | Writes demo artifacts, events, and status JSON files in output directory. | `none` | None. | `artifact_writes` | `DEMO_ONLY_VERIFIED` | the command name says `demo` |
| `builder-session` | Tier 1 — artifact-only planning/validation | `validation_only` | Delegates packaging and checks to subcommands. | No direct write authority at root CLI level. | `none` | Read-only checks or artifact-only packaging; no approval needed. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault` | Tier 1 — artifact-only planning/validation | `artifact_only` | Governed read-only CodeVault frame, lint, recall, context projection, deterministic bench, and validation; no shell, model, or CORE runtime. | Writes explicit artifact JSON paths only; no target-repo mutation. | `none` | Operator initiates scan and reviews emitted artifacts. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-agent` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Resolves active agent profiles and manifests. | No changes to workspace. | `explicit_operator_invocation` | Operator triggers agent inventory check. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-agent run` | Tier 3 — HITL-gated execution candidate | `read_only_runtime_candidate` | V.2: RO agent inspection candidate. Requires --read-only. Only profiles with authority=read_only (e.g. code_reviewer). No deepagents construction, shell, writes, MCP, or model invoke. | Writes RO receipt JSON only when an explicit output path is supplied. | `explicit_operator_invocation` | Operator must pass --read-only; refuses write/shell modes. | `artifact_writes` | `READ_ONLY_RUNTIME_VERIFIED` | promotion state is `read_only_runtime_candidate` |
| `builder-bridge` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Tests readiness of external deepagents integrations. | No changes to workspace. | `explicit_operator_invocation` | Operator checks network bridge connectivity. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-bundle` | Tier 1 — artifact-only planning/validation | `artifact_only` | Validates bundle definitions or packages build artifacts. | Creates ZIP or tar bundles in designated build folder. | `none` | Artifact build task. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-goose` | Tier 1 — artifact-only planning/validation | `validation_only` | Delegates validation and read-only audits to subcommands. | No direct write authority at root CLI level. | `none` | Validation and audit only. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-records` | Tier 1 — artifact-only planning/validation | `artifact_only` | Decodes and validates cryptographically signed or structured approval logs. | Writes verified signature files. | `none` | None; read-only verification. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-preflight` | Tier 1 — artifact-only planning/validation | `validation_only` | Runs local environment checks (Python version, CLI presence). | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-receipt` | Tier 1 — artifact-only planning/validation | `artifact_only` | Generates or reads execution receipts. | Writes receipt JSON files to output folders. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-chain` | Tier 1 — artifact-only planning/validation | `validation_only` | Traces artifact lineage chains. | Writes chain validation artifacts. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-handoff` | Tier 1 — artifact-only planning/validation | `artifact_only` | Aggregates verified evidence and creates handoff bundle metadata. | Writes handoff markdown files. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-intake` | Tier 1 — artifact-only planning/validation | `artifact_only` | Ingests inputs from outside workspace. | Writes configuration files in specific workspace location. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-index` | Tier 1 — artifact-only planning/validation | `artifact_only` | Tracks and indexes generated artifact files. | Updates local artifact index JSON ledger. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-promotion` | Tier 1 — artifact-only planning/validation | `validation_only` | Analyzes readiness for target promotion. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-promotion-decision` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a signed promotion decision artifact. | Writes promotion decision JSON artifact. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-state-index` | Tier 1 — artifact-only planning/validation | `artifact_only` | Constructs system state summaries. | Writes state index JSON files. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-snapshot` | Tier 1 — artifact-only planning/validation | `artifact_only` | Records current directory snapshot hashes. | Writes workspace snapshot hash index. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents` | Tier 4 — forbidden/unpromoted automation | `forbidden_unpromoted` | Delegates deepagent specs rendering and validation to subcommands; active run is forbidden. | No direct write authority at root CLI level. | `forbidden_unpromoted` | Forbidden; no supported approval path. | — | `BLOCKED_BY_EVIDENCE` | tier is `Tier 4` |
| `builder-notes` | Tier 1 — artifact-only planning/validation | `artifact_only` | Verifies or creates handoff notes artifacts. | Writes handoff notes markdown files. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-quality` | Tier 1 — artifact-only planning/validation | `validation_only` | Checks code linting or test coverage thresholds. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-research` | Tier 1 — artifact-only planning/validation | `artifact_only` | Builds read-only research plans. | Writes plan metadata files. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-performance` | Tier 0 — read-only inspection | `validation_only` | Measures CLI loading time and file sizes. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-readonly` | Tier 0 — read-only inspection | `read_only_runtime_candidate` | Inspects system files and configurations without execution. | No changes to workspace. | `none` | None. | — | `READ_ONLY_RUNTIME_VERIFIED` | promotion state is `read_only_runtime_candidate` |
| `builder-readonly policy` | Tier 1 — artifact-only planning/validation | `validation_only` | Generates read policy schema. | Writes policy JSON. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-readonly read` | Tier 1 — artifact-only planning/validation | `read_only_runtime_candidate` | Reads system files according to policy. | Writes read receipt JSON. | `none` | None. | `artifact_writes` | `READ_ONLY_RUNTIME_VERIFIED` | promotion state is `read_only_runtime_candidate` |
| `builder-readonly content-read` | Tier 1 — artifact-only planning/validation | `read_only_runtime_candidate` | Bounded explicit-path content capture that refuses any file whose content matches a secret pattern (best-effort denial, never a guarantee); no recursive discovery or glob expansion. | Writes content-read receipt JSON only under explicit output directory. | `none` | None. | `artifact_writes` | `READ_ONLY_RUNTIME_VERIFIED` | promotion state is `read_only_runtime_candidate` |
| `builder-readonly validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates read receipt JSON. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-verification` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates verification profile schemas. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-verify plan` | Tier 1 — artifact-only planning/validation | `artifact_only` | Generates a passive verification execution plan artifact only; no runtime start, shell execution, subprocess execution, model execution, MCP/tool invocation, Goose, deepagents, git mutation, or B2 patch authority. | Writes only the explicit verification execution plan JSON artifact requested by --output. | `none` | None. This is planned-only metadata and cannot authorize execution. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-verify validate-plan` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a verification execution plan artifact without runtime start, shell execution, subprocess execution, model execution, MCP/tool invocation, Goose, deepagents, git mutation, or B2 patch authority. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-platform operator-status` | Tier 1 — artifact-only planning/validation | `artifact_only` | Generates operator status report derived from truth matrix and command authority; no execution. | Writes status JSON artifact if output path requested. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-platform next` | Tier 1 — artifact-only planning/validation | `artifact_only` | Generates operator next action report derived from truth matrix and evidence gaps; no execution. | Writes next action JSON artifact if output path requested. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-platform operator-lane` | Tier 1 — artifact-only planning/validation | `artifact_only` | Composes config resolution, git snapshot, read/content receipts, context pack, routing recommendation, verification plan, golden path, and handoff; captures target git state via read-only `git rev-parse`/`git status` subprocesses; no model/MCP/commit authority. | Writes evidence artifacts only under explicit --output-dir. | `none` | None. | `artifact_writes`, `readonly_subprocess` | `BOUNDED_EXECUTION_VERIFIED` | `allows_readonly_subprocess` is set |
| `builder-platform golden-path` | Tier 1 — artifact-only planning/validation | `artifact_only` | Generates operator golden path report derived from status and next primitive; no execution. | Writes golden path JSON artifact if output path requested. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-platform final-loop-smoke` | Tier 1 — artifact-only planning/validation | `validation_only` | V.6: passive final operating loop smoke for selected targets (targets artifact → doctor → repo_map → context_pack → agent render → quality plan → handoff). No model execution, no free-form shell, no target mutation, no S3/S4 enablement. | Writes smoke artifacts under --output-dir only. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-platform validate-golden-path` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates operator golden path report; no execution. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-platform demo-loop` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Runs a guided governed demo against a temporary detached worktree of an operator-designated target repo (AssetOverflow/core remains a supported profile). It never commits, pushes, starts Goose, calls models, invokes MCP, or mutates the source checkout. | Writes demo evidence artifacts to --output-dir and may apply/rollback one approved temporary documentation marker patch inside the demo worktree only. | `explicit_operator_invocation` | Operator must explicitly pass --approve before the temporary demo worktree patch is applied. | `source_writes`, `git_mutation`, `artifact_writes` | `DEMO_ONLY_VERIFIED` | the command name says `demo` |
| `builder-platform validate-demo-loop` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a governed demo loop report artifact without runtime start, model execution, source writes, or git mutation. | No changes to workspace. | `none` | None. | — | `DEMO_ONLY_VERIFIED` | the command name says `demo` |
| `builder-platform wow` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Alias for the governed demo loop used for recording. It operates on a temporary detached worktree of the target repo only. | Writes demo evidence artifacts to --output-dir and may apply/rollback one approved temporary documentation marker patch inside the demo worktree only. | `explicit_operator_invocation` | Operator must explicitly pass --approve before the temporary demo worktree patch is applied. | `source_writes`, `git_mutation`, `artifact_writes` | `MUTATION_WITH_ROLLBACK_VERIFIED` | `allows_source_writes` is set |
| `builder-verify approve-plan` | Tier 1 — artifact-only planning/validation | `artifact_only` | Generates a digest-bound HITL approval artifact for a passive verification execution plan only; no runtime start, direct execution, shell execution, subprocess execution, model execution, MCP/tool invocation, Goose, deepagents, git mutation, or B2 patch authority. | Writes only the explicit verification execution approval JSON artifact requested by --output. | `none` | None. The artifact binds human approval to a plan digest only and does not authorize execution. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-verify validate-approval` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a verification execution approval artifact against its referenced passive plan without runtime start, direct execution, shell execution, subprocess execution, model execution, MCP/tool invocation, Goose, deepagents, git mutation, or B2 patch authority. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-verify validate-receipt` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a passive B1.3A verification execution receipt artifact against its referenced plan and approval without runtime start, direct execution, shell execution, subprocess execution, model execution, MCP/tool invocation, Goose, deepagents, git mutation, or B2 patch authority. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-verify evaluate-promotion` | Tier 1 — artifact-only planning/validation | `validation_only` | B2.0 machine-checkable promotion-gate evaluator: reads plan/approval/receipt (and optional ledger) artifacts, recomputes digest bindings and gate checks, and emits a passive promotion-evidence artifact. No subprocess, shell, model, MCP, Goose, deepagents, source writes, or matrix flips. | Writes only an optional explicit promotion-evidence JSON path when --output is provided. | `none` | None. Evidence of gate PASS is not operator approval and does not flip capability state. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-verify validate-promotion-evidence` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a builder_ii.verification_promotion_evidence artifact for schema, gate shape, and non-authority invariants. No execution. | No writes. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-verify run-approved` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Runs exactly one bounded verification profile from fixed in-code argv with subprocess shell=False, after validating the plan artifact, approval artifact, and approval-to-plan binding. Supported profiles are the safe builder-II-argv profiles platform_status and docs_audit, plus the target-code-executing profiles pytest_full and builder_full, which run the target repository's own test/conftest/plugin code and therefore require an execution-risk acknowledgment on the approval before the runner will spawn them. The per-profile timeout is read from the approved plan (range-checked to [1,1800]s). This bounds invocation only, never invoked-code behavior; it is not a sandbox. | Writes only the explicit verification execution receipt artifact requested by --output, and only under the configured artifact root when output is inside the target repository. A target-code profile may leave incidental pytest cache/bytecode byproducts in the target repo; these are recorded as observed byproducts and any non-ignored change invalidates the receipt. | `hitl_artifact_required` | Requires a digest-bound verification execution approval artifact for the requested command profile and step id; a target-code profile additionally requires the approval to carry an execution-risk acknowledgment. | `artifact_writes`, `readonly_subprocess` | `BOUNDED_EXECUTION_VERIFIED` | `allows_readonly_subprocess` is set |
| `builder-hitl` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Delegates execution request and receipt operations to subcommands. | No direct write authority at root CLI level. | `hitl_artifact_required` | Operator must sign hitl request and verify receipts. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-orchestration` | Tier 1 — artifact-only planning/validation | `artifact_only` | Delegates passive plan setup, assignment rendering, validation, and dry-run subcommands. | No direct write authority at root CLI level. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-profile-pack` | Tier 1 — artifact-only planning/validation | `artifact_only` | Delegates passive profile-pack scaffold, render, dry-run, and validation subcommands. | No direct write authority at root CLI level. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-model-policy` | Tier 1 — artifact-only planning/validation | `artifact_only` | Delegates passive model client registry and routing policy subcommands. | No direct write authority at root CLI level. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp` | Tier 1 — artifact-only planning/validation | `artifact_only` | Delegates passive WRP control-plane subcommands (classify/plan/allocate/gate/evaluate/replay). | No direct write authority at root CLI level. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-model` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Delegates governed model execution subcommands. | No direct write authority at root CLI level. | `none` | Delegated to subcommands. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-platform` | Tier 1 — artifact-only planning/validation | `validation_only` | Delegates passive completion-truth matrix, status, and docs-audit rendering; no runtime, model, shell, Goose, deepagents, MCP, or tool execution. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-memory` | Tier 1 — artifact-only planning/validation | `artifact_only` | Delegates governed memory atom, index, search, reconstruction, and validation subcommands without runtime execution, model calls, shell execution, hidden retrieval, or vector-store behavior. | No direct write authority at root CLI level; subcommands may write only explicit JSON memory artifacts when --output is supplied. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-config` | Tier 1 — artifact-only planning/validation | `artifact_only` | Delegates passive config schema rendering, source resolution, and validation; no runtime, model, shell, Goose, deepagents, MCP, tool, or patch execution. | No direct write authority at root CLI level; subcommands may write explicit JSON artifacts only when --output is supplied. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-govern` | Tier 1 — artifact-only planning/validation | `artifact_only` | Dispatches ratification point inspection, standing-grant minting/revocation, and ledger audit subcommands; no runtime, model, shell, subprocess, Goose, deepagents, MCP/tool invocation, or patch authority. | No direct write authority at root CLI level; subcommands write only ratification grant, revocation, and ledger artifacts under the ratification store root. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-govern list-points` | Tier 0 — read-only inspection | `spec_only` | Reads the ratification point registry, recomputes grant eligibility from this command-authority registry, and reads existing grant/revocation artifacts; no execution of any kind. | No changes to workspace, artifact store, runtime state, or target repository. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-govern grant-auto` | Tier 1 — artifact-only planning/validation | `artifact_only` | Mints a digest-bound standing ratification grant artifact for one registered, currently-eligible point and appends one chained ledger event; executes nothing and applies nothing. | Writes a ratification grant JSON and one ratification ledger line under the ratification store root only. | `none` | The operator must type the point id back at an interactive prompt, or pass --yes for scripted flows; --granted-by is required in both cases, so no grant exists without a named delegator. A point whose kind is human_approval_mint or promotion_decision, or whose owning command carries a capability outside the grantable set, is refused with no write. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-govern policy-show` | Tier 0 — read-only inspection | `spec_only` | Reads the ratification policy artifact and recomputes the effective level for every registered point; no execution of any kind. | No changes to workspace, artifact store, runtime state, or target repository. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-govern policy-set` | Tier 1 — artifact-only planning/validation | `artifact_only` | Mints a digest-bound ratification policy artifact and appends one chained ledger event; executes nothing and applies nothing. | Writes the ratification policy JSON and one ratification ledger line under the ratification store root only. | `none` | Requires --set-by. A declared level weaker than the registry baseline for that point is refused with no write; a policy may only tighten, and the effective level is max(baseline, declared) so a weaker level could not take effect even if written. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-govern policy-validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Re-validates a ratification policy artifact's schema, point ids, levels, tighten-only rule, and digest binding; no execution of any kind. | No changes to workspace, artifact store, runtime state, or target repository. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-govern approve` | Tier 1 — artifact-only planning/validation | `artifact_only` | Mints a digest-bound ratification approval artifact for one point and one exact subject digest after an interactive digest-prefix confirmation; executes nothing and applies nothing. | Writes an approval JSON to the explicit --output path and, where a ratification store already exists, one ratification ledger line. | `none` | The operator must transcribe the subject digest prefix at an interactive prompt; there is deliberately no non-interactive mode, because this artifact is evidence a human decided. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-govern validate-approval` | Tier 1 — artifact-only planning/validation | `validation_only` | Re-validates a ratification approval artifact's schema, registered point, timestamps, and digest binding; no execution of any kind. | No changes to workspace, artifact store, runtime state, or target repository. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-govern list-grants` | Tier 0 — read-only inspection | `spec_only` | Reads ratification grant and revocation artifacts and reports each grant's status; no execution of any kind. | No changes to workspace, artifact store, runtime state, or target repository. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-govern revoke` | Tier 1 — artifact-only planning/validation | `artifact_only` | Mints a digest-bound revocation artifact against an existing grant digest and appends one chained ledger event; executes nothing. | Writes a ratification revocation JSON and one ratification ledger line under the ratification store root only; the revoked grant file is preserved. | `none` | Requires --revoked-by and --reason; the grant digest must match a grant on file. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-govern validate-grant` | Tier 1 — artifact-only planning/validation | `validation_only` | Re-validates a ratification grant artifact's schema, kind, registered point id, and digest binding; no execution of any kind. | No changes to workspace, artifact store, runtime state, or target repository. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-govern ledger` | Tier 0 — read-only inspection | `spec_only` | Reads the ratification audit ledger JSONL and renders it; no execution of any kind. | No changes to workspace, artifact store, runtime state, or target repository. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-govern validate-ledger` | Tier 1 — artifact-only planning/validation | `validation_only` | Recomputes every ledger entry digest and re-checks sequence and prev_digest continuity; no execution of any kind. | No changes to workspace, artifact store, runtime state, or target repository. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-govern trace` | Tier 0 — read-only inspection | `spec_only` | Reads the ratification point registry, grant store, and ledger to report one point's full decision history; no execution of any kind. | No changes to workspace, artifact store, runtime state, or target repository. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-govern consult` | Tier 0 — read-only inspection | `spec_only` | Recomputes grant eligibility and reads the grant store to report whether a point is currently satisfied; no execution of any kind. | No changes to workspace, artifact store, runtime state, or target repository. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-setup` | Tier 1 — artifact-only planning/validation | `artifact_only` | Delegates passive setup plan, overlay plan, rollback snapshot, and validation subcommands; no setup apply, rollback execution, Goose start, model call, shell execution, deepagents runtime, MCP/tool invocation, or patch authority. | No direct write authority at root CLI level; artifact commands write only explicit JSON output artifacts when --output is supplied. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-workflow` | Tier 1 — artifact-only planning/validation | `artifact_only` | Delegates passive workflow state-machine subcommands; no runtime, model, shell, Goose, deepagents, or MCP execution. | Writes governed workflow artifacts only under explicit workflow output directories. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-ledger` | Tier 1 — artifact-only planning/validation | `artifact_only` | Delegates event-ledger list, replay, audit, export, and verification execution ledger query/validation/reconstruction subcommands without executing runtime work. | Writes replay and ledger export artifacts only when requested by ledger subcommands. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-session prepare-package` | Tier 1 — artifact-only planning/validation | `artifact_only` | Prepares context packaging and checks files. | Writes prepared package files locally. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-session validate-prepare-package` | Tier 1 — artifact-only planning/validation | `validation_only` | Performs validations on the prepared package directory structure. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-session summarize-prepare-package` | Tier 1 — artifact-only planning/validation | `validation_only` | Analyzes prepared package and generates a passive summary. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-context pack` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Invokes legacy external scanner or git commands. | Writes context bundle files. | `explicit_operator_invocation` | Explicit operator invocation only; no artifact approval chain. | `artifact_writes`, `external_tool_invocation` | `BOUNDED_EXECUTION_VERIFIED` | `allows_external_tool_invocation` is set |
| `builder-context changed` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Queries git status or git diff via subprocess. | No changes to workspace. | `explicit_operator_invocation` | Explicit operator invocation only; no artifact approval chain. | `readonly_subprocess`, `external_tool_invocation` | `BOUNDED_EXECUTION_VERIFIED` | `allows_external_tool_invocation` is set |
| `builder-context artifact` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Processes codebase scanning, potentially using external tools like repomix. | Creates context artifact files. | `explicit_operator_invocation` | Explicit operator invocation only; no artifact approval chain. | `artifact_writes`, `external_tool_invocation` | `BOUNDED_EXECUTION_VERIFIED` | `allows_external_tool_invocation` is set |
| `builder setup` | Tier 1 — artifact-only planning/validation | `validation_only` | Fail-closed compatibility wrapper that redirects operators to governed builder-setup plan/overlay/rollback/apply commands; no setup apply is performed by this legacy surface. | No changes to workspace, target repo, Goose config, .goosehints, skills, recipes, or runtime state. | `none` | None. The command is informational only and exits non-zero after printing the governed migration path. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder onboarding` | Tier 1 — artifact-only planning/validation | `artifact_only` | Interactive guided onboarding flow delegating directly to governed builder-setup wizard; no Goose, model, MCP, deepagents, patch, or shell execution. | Writes passive onboarding/setup artifacts under explicit output directory only; no setup mutation or receipt generation. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder init` | Tier 1 — artifact-only planning/validation | `artifact_only` | Unified onboarding orchestrator composing the governed onboarding pipeline (setup plan, overlay plan, rollback snapshot, intent report); prompts all nine onboarding decisions and registry-validates every answer, typed or flag-provided; no Goose, model, MCP, deepagents, patch, or shell execution. | Writes passive onboarding/setup artifacts under the selected output directory only; never applies setup and never writes a receipt. | `none` | None. Prints the follow-up builder-setup apply command without an inline digest; apply approval happens only in the separately invoked apply step. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder pull` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Runs model-cache download helpers for explicitly selected local model tiers. | Writes model cache files outside the target repository through operator-invoked helper scripts. | `explicit_operator_invocation` | Explicit operator invocation only; no autonomous download or runtime authority. | `state_writes`, `external_tool_invocation` | `BOUNDED_EXECUTION_VERIFIED` | `allows_external_tool_invocation` is set |
| `builder start` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Starts background runtime processes and servers. | Creates runtime process state and session context artifacts only; does not perform legacy setup writes. | `explicit_operator_invocation` | Explicit operator invocation only; no artifact approval chain. | `runtime_start`, `state_writes`, `external_tool_invocation` | `READ_ONLY_RUNTIME_VERIFIED` | `allows_runtime_start` is set |
| `builder ask` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Queries model provider or MLX local runtime using user input. | Writes conversation history files locally. | `explicit_operator_invocation` | Explicit operator invocation only; no artifact approval chain. | `model_execution`, `artifact_writes` | `LIVE_PROVIDER_VERIFIED` | `allows_model_execution` is set |
| `builder verify` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Invokes local pytest/runner test suites via subprocess. | No source code changes; generates test result files. | `explicit_operator_invocation` | Explicit operator invocation only; no artifact approval chain. | `readonly_subprocess`, `external_tool_invocation` | `BOUNDED_EXECUTION_VERIFIED` | `allows_external_tool_invocation` is set |
| `builder benchmark` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Runs bounded local benchmark probes and may call the configured chat-completions endpoint if reachable. | Writes a benchmark report only when an explicit output path is provided. | `explicit_operator_invocation` | Explicit operator invocation only; no autonomous benchmark or model probing. | `model_execution`, `artifact_writes`, `external_tool_invocation` | `LIVE_PROVIDER_VERIFIED` | `allows_model_execution` is set |
| `builder capabilities` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Checks served-model capability gates and optionally runs a live chat smoke when --chat is passed. | No changes to workspace, target repository, or runtime state. | `explicit_operator_invocation` | Explicit operator invocation only; --chat is a live model probe. | `model_execution`, `external_tool_invocation` | `LIVE_PROVIDER_VERIFIED` | `allows_model_execution` is set |
| `builder switch-model` | Tier 1 — artifact-only planning/validation | `validation_only` | Normalizes model aliases and prints environment lines only. | No changes to workspace, environment files, runtime state, or target repository. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder models` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Inspects local model cache directories with bounded read-only filesystem and size probes. | No changes to workspace, cache contents, or target repository. | `explicit_operator_invocation` | Explicit operator invocation only. | `readonly_subprocess` | `BOUNDED_EXECUTION_VERIFIED` | `allows_readonly_subprocess` is set |
| `builder doctor` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Runs local readiness probes across repo path, Goose availability, backend health, recipes, compliance, and model cache state. | No changes to workspace, runtime state, or target repository. | `explicit_operator_invocation` | Explicit operator invocation only. | `readonly_subprocess`, `external_tool_invocation` | `BOUNDED_EXECUTION_VERIFIED` | `allows_external_tool_invocation` is set |
| `builder status` | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | Reads backend health, Goose status, recipe validation, compliance, and local model cache status. | No changes to workspace, runtime state, or target repository. | `explicit_operator_invocation` | Explicit operator invocation only. | `readonly_subprocess`, `external_tool_invocation` | `BOUNDED_EXECUTION_VERIFIED` | `allows_external_tool_invocation` is set |
| `builder chain` | Tier 0 — read-only inspection | `spec_only` | Reads the command-authority registry and the ratification point registry and prints the governed patch loop stage by stage; runs no stage, spawns no subprocess, and has no runtime, model, shell, Goose, deepagents, MCP, or patch authority. | No changes to workspace, artifact store, runtime state, or target repository. | `none` | None: it performs no stage, so there is nothing to approve. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder onboard` | Tier 1 — artifact-only planning/validation | `artifact_only` | Reads onboarding filesystem markers and the ratification point registry, then mints only the standing ratification grants the operator explicitly accepts at the prompt; runs no onboarding command, and has no runtime, model, shell, subprocess, Goose, deepagents, MCP, or patch authority. | Writes ratification grant artifacts and ratification ledger lines under the ratification store root only; touches no setup target, no session, and no source file. | `none` | Each grant requires a per-point interactive confirmation plus a recorded delegator name; --no-prompt describes every point and writes nothing. Points whose kind is human_approval_mint or promotion_decision are reported as non-delegable and are never offered. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder next` | Tier 1 — artifact-only planning/validation | `validation_only` | Reads REQUIRED_CAPABILITY_ROWS and onboarding filesystem markers (.env, .builder/artifacts, .builder/session) via os.path.exists / Path.exists checks only. | No changes to workspace, runtime state, artifact store, or target repository. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder course` | Tier 1 — artifact-only planning/validation | `validation_only` | Reads onboarding filesystem markers via get_onboarding_state (.env, .builder/artifacts, .builder/session existence checks only). | No changes to workspace, runtime state, artifact store, or target repository. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder config` | Tier 1 — artifact-only planning/validation | `validation_only` | Renders passive configuration and legacy setup redirect metadata. | No changes to workspace, runtime state, or target repository. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder init-prompt` | Tier 1 — artifact-only planning/validation | `validation_only` | Renders the governed initialization prompt text. | No changes to workspace, runtime state, or target repository. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-goose manifest` | Tier 1 — artifact-only planning/validation | `validation_only` | Inspects Goose configuration manifest templates. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-goose validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Performs validations on active Goose session configs. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-goose readonly-audit` | Tier 1 — artifact-only planning/validation | `validation_only` | Ensures a target Goose session uses read-only tools only. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-goose validate-audit` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates the output of a Goose audit run. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-goose inspect-readonly` | Tier 1 — artifact-only planning/validation | `validation_only` | Performs read-only inspection validation on explicitly requested paths. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-goose validate-inspection` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates inspection config details. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-goose start-readonly` | Tier 3 — HITL-gated execution candidate | `read_only_runtime_candidate` | Starts a Goose session bounded by read-only policies. | No writes permitted. Strict environment isolation. | `explicit_operator_invocation` | Requires implicit or explicit HITL approval for launch. | — | `READ_ONLY_RUNTIME_VERIFIED` | promotion state is `read_only_runtime_candidate` |
| `builder-goose close-readonly` | Tier 3 — HITL-gated execution candidate | `read_only_runtime_candidate` | Closes a governed Goose session and verifies no mutation occurred. | No writes permitted. Target filesystem is inspected for drift. | `explicit_operator_invocation` | Delegated to CLI lifecycle. | — | `READ_ONLY_RUNTIME_VERIFIED` | promotion state is `read_only_runtime_candidate` |
| `builder-deepagents policy` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders governed deepagents policy metadata statically. | Writes policy JSON only to explicit artifact output paths. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates deepagents spec metadata. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents readiness` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders optional deepagents dependency-readiness metadata. | Writes readiness JSON only to explicit artifact output paths. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents validate-readiness` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates deepagents dependency-readiness metadata. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents forge` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates or previews governed deepagent profile artifacts only; no native deepagents construction, model execution, shell execution, MCP/tool invocation, Goose activation, source writes outside bounded Forge artifacts, git mutation, or runtime promotion. | Dry-run writes nothing. Real emission writes only profiles/deepagents/{slug}.yaml and an optional profiles/deepagents/forge_{slug}.handoff.json handoff artifact. | `explicit_operator_invocation` | Operator must explicitly invoke Forge; emitted artifacts are not runtime approval or promotion authority. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents run-readonly` | Tier 3 — HITL-gated execution candidate | `read_only_runtime_candidate` | V.2: RO agent inspection via shared agent_readonly_runner (read_only profiles only). Does not construct deepagents, invoke delegate, shell, write, or call models. | Writes RO receipt JSON only when an explicit output path is supplied. | `explicit_operator_invocation` | Operator invokes run-readonly; not autonomous multi-agent. | `artifact_writes` | `READ_ONLY_RUNTIME_VERIFIED` | promotion state is `read_only_runtime_candidate` |
| `builder-deepagents delegate` | Tier 4 — forbidden/unpromoted automation | `forbidden_unpromoted` | Attempts to execute autonomous models. Forbidden. | No changes to workspace. | `forbidden_unpromoted` | Forbidden; no supported approval path. | — | `BLOCKED_BY_EVIDENCE` | tier is `Tier 4` |
| `builder-deepagents work-plan` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive deepagents work plan statically. | Writes work plan JSON only to explicit artifact output paths. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents assign-subagent` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive subagent assignment statically. | Writes subagent assignment JSON only to explicit artifact output paths. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents record-result` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive subagent result statically. | Writes subagent result JSON only to explicit artifact output paths. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents review-result` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive subagent review statically. | Writes subagent review JSON only to explicit artifact output paths. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents request-human-gate` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive human gate request statically. | Writes human gate request JSON only to explicit artifact output paths. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents record-blocked-action` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive blocked action record statically. | Writes blocked action record JSON only to explicit artifact output paths. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents proposal-result` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive proposal result statically. | Writes proposal result JSON only to explicit artifact output paths. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents validate-work-artifact` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates passive deepagents work artifact metadata. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents backend-readiness` | Tier 1 — artifact-only planning/validation | `artifact_only` | Inspects the optional_deepagents protocol adapter exports and records a promotion-readiness gate without constructing an agent. | Writes backend readiness gate JSON only to explicit artifact output paths. | `explicit_operator_invocation` | Operator must explicitly assert AGENTS.md capability gates before the gate can pass. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents execution-candidate` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a bounded deepagents protocol execution candidate without running a backend; optional_deepagents requires a passing backend-readiness gate. | Writes candidate JSON only to explicit artifact output paths. | `none` | None; later run-approved requires digest-bound HITL approval. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents approve-candidate` | Tier 1 — artifact-only planning/validation | `artifact_only` | Binds human approval to the exact candidate digest via required --approval-actor/--approval-reason flags (non-interactive; no typed-prefix prompt); the digest-bound approval artifact seals the whole envelope (lane policy, root budget, allowed obligation kinds, refused lanes) when the candidate declares one. | Writes approval JSON only to explicit artifact output paths. | `explicit_operator_invocation` | Human actor and reason are required inputs; --native-backend-acknowledged is the second key required to seal an optional_deepagents obligation-bearing candidate. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents run-approved` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Runs the approved protocol backend lane only after candidate and approval digests bind. With --obligation, each subagent runs its OWN mint-checked obligation task and each discharge is classified (CONTRACT_SATISFIED / DISCHARGED_UNVERIFIED / CONTRACT_VIOLATED / BLOCKED). | Writes run envelope, hash-chained events (including obligation_minted / obligation_mint_refused / obligation_consumed), replay report, ledger, receipt, and optional checkpoint under the approved output root. | `hitl_artifact_required` | Requires a valid builder_ii.deepagents_execution_approval bound to the exact candidate; an obligation-bearing run additionally requires a Ladder 4 seal and, for optional_deepagents, the two-key native acknowledgement. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents replay-run` | Tier 1 — artifact-only planning/validation | `validation_only` | Reconstructs deepagents run state from event records only. | Writes replay report JSON only to explicit artifact output paths. | `none` | None; replay never reruns backend/model/tool work. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents evidence-bundle` | Tier 1 — artifact-only planning/validation | `artifact_only` | Bundles deepagents candidate, approval, run, receipt, ledger, and replay evidence. | Writes evidence bundle JSON only to explicit artifact output paths. | `none` | None; evidence bundle does not grant authority. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents resume-approved` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Resumes a checkpointed protocol backend run only when candidate and approval still bind exactly. | Appends hash-chained events and rewrites run envelope, replay, ledger, and receipt under the approved output root. | `hitl_artifact_required` | Requires the original approval and checkpoint bound to the same candidate. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents run-plan` | Tier 1 — artifact-only planning/validation | `artifact_only` | Synthesizes legacy passive planning artifacts; not the promoted approved lane. | Writes synthetic runtime envelope and subagent receipts only to explicit artifact paths. | `explicit_operator_invocation` | Operator explicitly invokes the compatibility command; no approval artifact is consumed. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-deepagents collect-results` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Collects subagent results into a final proposal result. | Writes proposal result JSON only to explicit artifact output paths. | `explicit_operator_invocation` | Delegated to CLI invocation. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-hitl propose-patch` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a design-only patch proposal artifact. | Writes patch proposal JSON file to specified output. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-hitl approve-patch` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders a proposal's diff and patch digest, then mints a digest-bound approval artifact after an interactive digest-prefix confirmation. No patch is applied and no source is written. | Writes an approval JSON file to the specified output path only. | `none` | The operator must transcribe the patch-digest prefix at an interactive prompt; there is no non-interactive approval mode. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-hitl refuse-patch` | Tier 1 — artifact-only planning/validation | `artifact_only` | Records a passive refusal of a schema-valid patch proposal. No approval is minted, no patch is applied, and no source is written. Distinct from promotion rejection-record (wrong ceremony for patch proposals). | Writes a refusal JSON file to the specified output path only. | `none` | None — refusal is an operator-recorded non-approval, not an authority grant. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-hitl apply-patch` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Applies a patch specifically from an approved proposal to a clean repository after rollback plan creation. | Modifies source tree according to the diff. Writes postflight and receipt records. | `hitl_artifact_required` | Requires a schema-valid, unexpired hitl_patch_approval artifact bound to the proposal's content and patch digests, verified inside apply_hitl_patch. Digest-chained binding, not a cryptographic signature. | `source_writes`, `artifact_writes` | `MUTATION_WITH_ROLLBACK_VERIFIED` | `allows_source_writes` is set |
| `builder-hitl approve-rollback` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders a rollback plan's summary and digest, then mints a plan-bound rollback approval artifact after an interactive digest-prefix confirmation. No rollback is executed and no source is written. | Writes a rollback approval JSON file to the specified output path only. | `none` | The operator must transcribe the rollback-plan-digest prefix at an interactive prompt; there is no non-interactive approval mode. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-hitl rollback` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Restores the codebase to its pre-apply state via `git apply -R` of the stored forward patch, after a working-tree drift preflight. | Modifies the source tree via `git apply -R` only; writes a rollback receipt carrying post-rollback equivalence evidence. | `hitl_artifact_required` | Requires a schema-valid, unexpired hitl_rollback_approval artifact bound to the rollback plan's content and patch digests (not merely the machine-generated rollback plan), verified inside rollback_hitl_patch. | `source_writes`, `artifact_writes` | `MUTATION_WITH_ROLLBACK_VERIFIED` | `allows_source_writes` is set |
| `builder-hitl run-command` | Tier 4 — forbidden/unpromoted automation | `forbidden_unpromoted` | Fail-closed. Arbitrary approved command execution is disabled until rebuilt as fixed-profile bounded runner. | No execution; request/receipt artifact commands remain available separately. | `forbidden_unpromoted` | Use builder-verify run-approved for bounded execution. | — | `BLOCKED_BY_EVIDENCE` | tier is `Tier 4` |
| `builder-hitl request` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Collects and validates HITL request details. | Writes HITL request JSON artifact. | `hitl_artifact_required` | Requires explicit operator approval signature. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-hitl receipt` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Records execution completion or failure receipt metadata. | Writes HITL receipt JSON artifact. | `hitl_artifact_required` | Operator must sign hitl request and verify receipts. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-hitl validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates request and receipt artifact files against schema. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-hitl validate-decision-envelope` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a hitl_decision_envelope artifact against schema and reports whether any criterion is out of its acceptable range. | No changes to workspace. | `none` | None. A decision envelope is evidence a human weighs; it approves nothing. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-hitl promotion-request` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive promotion request statically. | Writes promotion request JSON only to explicit artifact output paths. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-hitl promotion-review` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive promotion review statically. | Writes promotion review JSON only to explicit artifact output paths. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-hitl promotion-decision` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive promotion decision statically. | Writes promotion decision JSON only to explicit artifact output paths. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-hitl approval-boundary` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive approval boundary statically. | Writes approval boundary JSON only to explicit artifact output paths. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-hitl rejection-record` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive rejection record statically. | Writes rejection record JSON only to explicit artifact output paths. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-hitl validate-promotion` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates passive promotion bridge artifact files against schema. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-hitl candidate-manifest` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive execution candidate manifest statically. | Writes candidate manifest JSON only to explicit artifact output paths. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-hitl validate-candidate-manifest` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates passive candidate manifest and validation report files against schema. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-orchestration plan` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates plan structure statically without launching active agents. | Writes plan JSON file. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-orchestration render-assignment` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive Goal 2 assignment and orchestration plans from existing source artifacts and SHA-256 refs. | Writes assignment/orchestration JSON only to explicit artifact output paths. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-orchestration validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates v1 orchestration and Goal 2 assignment/orchestration artifacts. | Writes validation-report JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-orchestration dry-run` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a passive dry-run explaining Goal 2 planned bindings without executing anything. | Writes dry-run JSON only to an explicit artifact output path. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-orchestration lane-policy` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders the Ladder 4 obligation lane policy from a fixed in-code table and checks each command-form discharge mechanism against COMMAND_AUTHORITY_REGISTRY (read-only). | Writes lane policy JSON only to an explicit artifact output path. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-orchestration validate-lane-policy` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates an orchestration lane policy artifact file (schema, totality, digest) and re-checks command-form discharge mechanisms against COMMAND_AUTHORITY_REGISTRY (read-only). | No writes; reads and validates an existing artifact file only. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-orchestration mint-obligation` | Tier 1 — artifact-only planning/validation | `artifact_only` | Builds a governed obligation artifact statically: derives and checks the lane against a supplied lane policy, pins its digest, and validates the result. Does not run, spawn, or enforce a runtime budget envelope. | Writes obligation JSON only to an explicit artifact output path. | `none` | None. The obligation is inert until a separately sealed runner consumes it (Ladder 4 PR-4). | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-orchestration validate-obligation` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates an orchestration obligation artifact file: schema, anti-dump, parent_ref XOR, budget shape, and digest re-derivation (tamper detection). | No writes; reads and validates an existing artifact file only. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-orchestration status` | Tier 1 — artifact-only planning/validation | `validation_only` | Deterministic read-only walk over a `builder-deepagents run-approved --obligation` output directory: re-derives the event chain fresh from the per-event files (tamper-sensitive) and renders one row per obligation with its board state (OPEN / SATISFIED / UNVERIFIED / VIOLATED / BLOCKED) and granted budget partition. No model, no execution. | Writes the rendered status-board JSON only to an explicit --output artifact path; never writes to the run directory it inspects. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-orchestration why` | Tier 1 — artifact-only planning/validation | `validation_only` | Deterministic read-only belief trace for one obligation, located from one of its lifecycle event files: re-walks the whole run fresh from disk and reports believed?/required evidence/attached/consumed. Only a CONTRACT_SATISFIED discharge over an intact chain is believed. No model, no execution. | Writes the belief-trace JSON only to an explicit --output artifact path; never writes to the run directory it inspects. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-profile-pack scaffold` | Tier 1 — artifact-only planning/validation | `artifact_only` | Scaffolds passive profile-pack manifests from local source refs and hashes only. | Writes profile-pack manifest JSON only to an explicit artifact output path. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-profile-pack wizard` | Tier 1 — artifact-only planning/validation | `artifact_only` | Prompts the four scaffold decisions, registry-validating the target profile against the live registry at prompt time, then emits exactly what `builder-profile-pack scaffold` emits. | Writes profile-pack manifest JSON only to an explicit artifact output path. | `none` | None. It plans a manifest; rendering and applying are separate commands. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-profile-pack render` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders a passive profile-pack render plan without materializing runtime behavior. | Writes render-plan JSON only to an explicit artifact output path. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-profile-pack validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates profile-pack lifecycle artifacts and can emit a passive validation report. | Writes validation-report JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-profile-pack dry-run` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a passive dry-run artifact showing what would render without executing anything. | Writes dry-run JSON only to an explicit artifact output path. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-model-policy validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates passive model client registry, routing policy, and recommendation artifacts. | Writes validation report JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-model-policy render` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders passive model routing recommendation artifacts from registry and policy metadata. | Writes recommendation JSON only to an explicit artifact output path. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-model-policy dry-run` | Tier 1 — artifact-only planning/validation | `artifact_only` | Performs a passive routing recommendation dry-run showing recommended models without executing anything. | Writes dry-run recommendation JSON only to an explicit artifact output path. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-model-policy price-book` | Tier 1 — artifact-only planning/validation | `artifact_only` | Emits a RECORDED_ONLY builder_ii.price_book artifact with per-model rates and tokenizer ids. | Writes price-book JSON only to an explicit artifact output path. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-model-policy validate-price-book` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a builder_ii.price_book artifact. | Does not write artifacts. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp classify` | Tier 1 — artifact-only planning/validation | `artifact_only` | Classifies workloads into local tier recommendations using deterministic features only. | Writes classification JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp score-classifier` | Tier 1 — artifact-only planning/validation | `validation_only` | Scores golden classifier fixtures against the W0 ≥95% acceptance criterion. | Writes score report JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp plan-collab` | Tier 1 — artifact-only planning/validation | `artifact_only` | Emits Maker/Governor collaboration topology plans with UNBOUND runtime binding. | Writes topology JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp allocate` | Tier 1 — artifact-only planning/validation | `artifact_only` | Emits constrained fleet allocation recommendations under token budget and risk caps. | Writes allocation JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp gate` | Tier 1 — artifact-only planning/validation | `validation_only` | Evaluates MSDA declarative access gates; never grants execution. | Writes gate decision JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp msda-policy` | Tier 1 — artifact-only planning/validation | `artifact_only` | Emits default MSDA deny-by-default policy artifacts. | Writes policy JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp route` | Tier 1 — artifact-only planning/validation | `artifact_only` | Composes forward operator R recommendations from classifier, topology, allocation, and graph. | Writes forward route JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp evaluate` | Tier 1 — artifact-only planning/validation | `validation_only` | Evaluates trajectory quality/safety/sequence metrics as validation artifacts. | Writes evaluation JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp simulate-epochs` | Tier 1 — artifact-only planning/validation | `validation_only` | Runs synthetic MAAP epoch harness for W4 error-reduction acceptance. | Writes epoch report JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp replay` | Tier 1 — artifact-only planning/validation | `validation_only` | W5 reconstructive replay: planned digests/order plus optional commit_id/tree_hash repo-state binding vs observed chain; perfect_match requires both. | Writes replay report JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp graph` | Tier 1 — artifact-only planning/validation | `artifact_only` | Emits planned subtask graph artifacts (pure-Python DAG; no LangGraph dependency). Optional --bind-repo embeds cwd commit_id/tree_hash for W5 (read-only git). | Writes graph JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp package-exchange` | Tier 1 — artifact-only planning/validation | `artifact_only` | Writes Maker exchange packages for Antigravity Governor review (file-mediated). | Writes exchange manifests under an explicit root path only. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates WRP artifacts by kind against envelope, digest, and non-authority rules. | Writes validation report JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp proof` | Tier 1 — artifact-only planning/validation | `validation_only` | Emits Class R/D/U proof records as validation artifacts. | Writes proof JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp experience-init` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates empty experience store artifacts (MAAP reference; recorded only). | Writes store JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp adjoint` | Tier 1 — artifact-only planning/validation | `artifact_only` | Records adjoint R* corrections as recommendations requiring HITL promotion to apply. | Writes correction and optional updated store JSON only to explicit paths. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp plan-live` | Tier 1 — artifact-only planning/validation | `artifact_only` | Emits digest-bound WRP live run plan; does not execute. | Writes plan JSON only when an explicit output path is supplied. | `none` | None; execution requires approve-live + run-approved. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp approve-live` | Tier 1 — artifact-only planning/validation | `artifact_only` | Emits digest-bound HITL approval for a live run plan; does not execute. | Writes approval JSON only when an explicit output path is supplied. | `none` | Operator supplies --approved-by; approval is not execution. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp run-approved` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | S2 HITL live lane: forced MSDA preflight + graph execute. v1: noop|record only. v2: optional model_gateway|tool_gateway with default gateway_mode=record (no network/cloud provider); stub_tool is B7 allowlist only. No shell. | Writes live run receipt JSON only when an explicit output path is supplied. | `explicit_operator_invocation` | Requires digest-bound builder_ii.wrp.live_run_approval matching plan.digest. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp phi-policy-init` | Tier 1 — artifact-only planning/validation | `artifact_only` | Emits version-0 phi_policy from DEFAULT_PHI; does not change live routing defaults. | Writes policy JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp corrections-from-receipts` | Tier 1 — artifact-only planning/validation | `artifact_only` | Maps real receipts into experience exemplars and R* adjoint_correction artifacts; does not apply φ. | Writes store/corrections JSON only when explicit output paths are supplied. | `none` | None; φ apply requires plan-rstar-apply + approve + apply-rstar-approved. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp plan-rstar-apply` | Tier 1 — artifact-only planning/validation | `artifact_only` | Emits digest-bound R* apply plan aggregating correction digests into proposed φ; does not apply. | Writes plan JSON only when an explicit output path is supplied. | `none` | None; apply requires approve-rstar-apply + apply-rstar-approved. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp approve-rstar-apply` | Tier 1 — artifact-only planning/validation | `artifact_only` | Emits digest-bound HITL approval for an R* apply plan; does not apply φ. | Writes approval JSON only when an explicit output path is supplied. | `none` | Operator supplies --approved-by; approval is not apply. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp apply-rstar-approved` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | P4 HITL R* apply: digest-bound plan+approval → new versioned phi_policy artifact. Never mutates DEFAULT_PHI or live routing defaults; classifier must bind explicitly. | Writes phi_policy and/or apply receipt JSON only when explicit output paths are supplied. | `explicit_operator_invocation` | Requires digest-bound builder_ii.wrp.rstar_apply_approval matching plan.digest. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp benchmark` | Tier 1 — artifact-only planning/validation | `validation_only` | P5 Class U harness: local S2 v2 gateway scenarios with measured wall time / peak RSS; emits class_u_report + proof_record U + performance_measurement rows. Does not enable S3, cloud providers, or shell. | Writes report/proof/measurement JSON only when explicit output paths are supplied. | `none` | None; harness is validation-only measurement. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp langgraph-project` | Tier 1 — artifact-only planning/validation | `artifact_only` | P6: pure LangGraph-shaped projection of TrajectoryGraph (always available). Optional --compile requires BUILDER_II_WRP_LANGGRAPH=1 and importable/injected compiler; fail-closed otherwise. Never default runtime; never grants model/tool authority. | Writes projection/compile-handle JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp vllm-profile` | Tier 1 — artifact-only planning/validation | `artifact_only` | P6: emit vLLM WRP research profile status metadata. Never starts an engine, never imports torch/vLLM by default, never becomes default runtime path. | Writes profile status JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp opa-eval` | Tier 1 — artifact-only planning/validation | `validation_only` | P6: MSDA gate evaluation via pure-Python backend (default) or optional external opa binary (--backend opa). Never executes tools/models; optional opa fails closed when missing. | Writes decision JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp embed-status` | Tier 1 — artifact-only planning/validation | `artifact_only` | P6: report resolved WRP embedder (HashingEmbedder default; ModernBERT only when BUILDER_II_WRP_EMBEDDER=modernbert). Does not load heavy weights by default. | Writes status JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp repo-state` | Tier 1 — artifact-only planning/validation | `validation_only` | W5: capture git commit_id + tree_hash for a cwd (or honest nulls when not a git tree). Read-only git rev-parse only; shell=False fixed argv. | Writes repo-state JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp handoff-measure` | Tier 1 — artifact-only planning/validation | `validation_only` | W1: measure pure-Python collaboration handoff overhead (topology validate + handoff_route execute with zero-loss keys). Local scope only; not network dual-platform SLA. | Writes measurement JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp plan-agent-lifecycle` | Tier 1 — artifact-only planning/validation | `artifact_only` | AgentFactory plan-only: emit register_plan/retire_plan artifacts with spawn_permitted=false and UNBOUND runtime. No process/agent spawn. | Writes plan JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp agent-factory` | Tier 1 — artifact-only planning/validation | `validation_only` | Delegates AgentFactory spawn/retire/prove lifecycle record subcommands. | No direct write authority at root agent-factory group level. | `none` | Delegated to subcommands. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp agent-factory spawn` | Tier 1 — artifact-only planning/validation | `validation_only` | W.5: emit agent lifecycle spawn record with role/task binding and optional ExperienceStore digest linkage. Default unearned (spawn_executed=false, UNBOUND); optional seam_execution evidence earns SEAM_BOUND. Not OS process spawn; not global S3. | Writes lifecycle record JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp agent-factory retire` | Tier 1 — artifact-only planning/validation | `validation_only` | W.5: emit agent lifecycle retire record bound to prior spawn digest. Does not kill OS processes; inherits spawn_executed/runtime_binding from spawn. | Writes lifecycle record JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp agent-factory prove` | Tier 1 — artifact-only planning/validation | `validation_only` | W.5: deterministic spawn/retire lifecycle proof with ExperienceStore exemplar linkage. Pure records; no process spawn; no S3 enablement. | Writes agent_lifecycle_proof JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp msda-status` | Tier 1 — artifact-only planning/validation | `validation_only` | H9 honesty: report whether BUILDER_II_WRP_MSDA_PREFLIGHT env enables global gateway preflight (default off). Live lane / gateway nodes force independently. | Writes status JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp backends` | Tier 1 — artifact-only planning/validation | `validation_only` | P6.1: list WRP backend inventory (hash/MSDA defaults + opt-in ModernBERT/OPA/LangGraph/vLLM research + policy surfaces). Never starts engines or grants authority. | Writes inventory JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp doctor-backends` | Tier 1 — artifact-only planning/validation | `validation_only` | P6.1: doctor WRP backends. ok=true when M1-safe default paths healthy; missing opt-in binaries (opa/langgraph/modernbert) report unavailable without failing doctor. | Writes doctor report JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp s4-readiness` | Tier 1 — artifact-only planning/validation | `validation_only` | Delegates S4 readiness draft list/draft subcommands (validation_only). | No direct write authority at group root. | `none` | Delegated to subcommands. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp s4-readiness list` | Tier 1 — artifact-only planning/validation | `validation_only` | List opt-in/research backends eligible for S4 readiness drafts. | Writes list JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp s4-readiness draft` | Tier 1 — artifact-only planning/validation | `validation_only` | W.6: emit per-backend S4 readiness + decision *draft* package (decision always blocked / PENDING_HUMAN). Optional --write-evidence under planning/evidence. Does not promote S4, start engines, or enable S3. | Writes draft package JSON when --output set; writes readiness/decision/gate audit files only with --write-evidence. | `none` | None. HUMAN eight-gate later per backend — drafts are not approval. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp fleet-fidelity` | Tier 1 — artifact-only planning/validation | `validation_only` | W.3: cross-check fleet_binding fidelity from fleet_allocation → live_run_plan (alias/budget/risk/binds_session_routing; optional model_gateway annotation). | Writes fidelity report JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-wrp patterns-prove` | Tier 1 — artifact-only planning/validation | `validation_only` | W.4: pure graph_runtime mastery proof for sequential, fan_out_fan_in, hierarchical, handoff, cyclic. No S2 live, no gateway_handler, no shell/model. | Writes pattern_mastery_report JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-semantic` | Tier 1 — artifact-only planning/validation | `validation_only` | Delegates semantic RO doctor/map/preview/validate subcommands. | No direct write authority at root CLI level. | `none` | Delegated to subcommands. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-semantic doctor` | Tier 1 — artifact-only planning/validation | `validation_only` | V.1: detect-only semantic/structural RO stack health (repo_map path + serena/ast-grep/rg presence). No rewrites, no target mutation, no shell codemods. | Writes doctor report JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-semantic map` | Tier 1 — artifact-only planning/validation | `artifact_only` | V.1: bounded read-only semantic/structural map via create_repo_map + optional in-process AST symbol counts. No Serena index; no subprocess search. | Writes map JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-semantic preview` | Tier 1 — artifact-only planning/validation | `validation_only` | V.1: dry-run path/role substring preview over semantic map. Never invokes Serena rewrite or ast-grep apply. | Writes preview JSON only when an explicit output path is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-semantic validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates semantic doctor/map/preview artifacts against RO non-authority rules. | No writes. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-model call` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Executes a governed model call using an enabled model client. | Writes model call envelope and receipt JSON to output paths. | `explicit_operator_invocation` | Delegated to CLI invocation. | `model_execution`, `artifact_writes` | `LIVE_PROVIDER_VERIFIED` | `allows_model_execution` is set |
| `builder-model validate-receipt` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a model call receipt artifact against its schema. | No target repo writes. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-model standalone-call` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Executes a governed model call without logging to the workflow ledger. Intended for testing and offline evaluation only; no session state is produced. | Writes model call envelope and receipt JSON to explicit output paths only. | `explicit_operator_invocation` | Delegated to CLI invocation. No ledger event is recorded; operator is responsible for audit trail outside of session context. | `model_execution`, `artifact_writes` | `LIVE_PROVIDER_VERIFIED` | `allows_model_execution` is set |
| `builder-platform matrix` | Tier 1 — artifact-only planning/validation | `validation_only` | Renders the static source-derived completion truth matrix as JSON after validating shape and authority references. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-platform status` | Tier 1 — artifact-only planning/validation | `validation_only` | Summarizes the completion truth matrix without inspecting runtime systems or target repositories. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-platform known-limitations` | Tier 1 — artifact-only planning/validation | `validation_only` | Renders the known-limitations document from the completion truth matrix after validating matrix shape; no verification, model, tool, Goose, deepagents, shell, or MCP behavior. | Writes only the explicit --output document path when provided; stdout rendering leaves the workspace untouched. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-platform audit-docs` | Tier 1 — artifact-only planning/validation | `validation_only` | Scans README.md and docs/**/*.md for false operational-completion claims without invoking external tools. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-platform r1-closure` | Tier 1 — artifact-only planning/validation | `artifact_only` | Generates config schema, resolution, setup plan, overlay plan, rollback snapshot, onboarding intent, and R1 closure report artifacts in the requested output directory without runtime execution. | Writes only explicit R1 closure and proof artifacts into the provided output directory. No target repository or source files are modified. | `none` | None. Generating closure and onboarding proof artifacts never applies mutation. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-platform validate-r1-closure` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates an R1 closure report artifact and its referenced R1 chain evidence files without executing runtime or model operations. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-memory atom` | Tier 1 — artifact-only planning/validation | `artifact_only` | Wraps one explicit validated source artifact as a governed memory atom without runtime execution, model authority, shell execution, hidden memory, or vector-store retrieval. | Writes one memory atom JSON artifact only to the explicit output path when --output is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-memory index` | Tier 1 — artifact-only planning/validation | `artifact_only` | Builds a deterministic memory index from explicit memory atom artifacts only; no runtime execution, model calls, shell execution, hidden retrieval, or vector-store behavior. | Writes one memory index JSON artifact only to the explicit output path when --output is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-memory search` | Tier 1 — artifact-only planning/validation | `artifact_only` | Searches an explicit memory index with deterministic lexical scoring only; no model calls, semantic retrieval, hidden memory, shell execution, or runtime behavior. | Writes one memory search result JSON artifact only to the explicit output path when --output is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-memory reconstruct` | Tier 1 — artifact-only planning/validation | `artifact_only` | Reconstructs reviewable context from an explicit memory index without runtime execution, model authority, shell execution, hidden retrieval, or target repo mutation. | Writes one memory reconstruction JSON artifact only to the explicit output path when --output is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-memory validate-atom` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a memory atom artifact without runtime execution, model calls, shell execution, or memory mutation. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-memory validate-index` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a memory index artifact without runtime execution, model calls, shell execution, or memory mutation. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-memory validate-reconstruction` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a memory reconstruction artifact without runtime execution, model calls, shell execution, or memory mutation. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-memory validate-search-result` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a memory search result artifact without runtime execution, model calls, shell execution, or memory mutation. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-config schema` | Tier 1 — artifact-only planning/validation | `artifact_only` | Renders the static generic-first config schema; no runtime, model, shell, Goose, deepagents, MCP, tool, or patch execution. | Writes schema JSON only to an explicit artifact output path when --output is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-config resolve` | Tier 1 — artifact-only planning/validation | `artifact_only` | Resolves passive config sources and validates path policy without running subprocesses or invoking runtime systems. | Writes resolution JSON only to an explicit artifact output path when --output is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-config validate` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates config schema or resolution artifacts without runtime, model, shell, Goose, deepagents, MCP, tool, or patch execution. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-setup plan` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a passive setup plan from resolved config; no setup apply, rollback, runtime start, model call, shell execution, Goose start, deepagents construction, MCP/tool invocation, or patch authority. | Writes setup plan JSON only to an explicit artifact output path when --output is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-setup validate-plan` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a passive setup plan artifact without applying setup, rollback, Goose, deepagents, model, MCP/tool, shell, or patch behavior. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-setup overlay-plan` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a passive setup overlay plan from a setup plan; runtime execution, model execution, shell execution, Goose runtime, deepagents runtime, MCP/tool invocation, patch authority, setup apply, and setup rollback execution are disabled. | Writes setup overlay JSON only to an explicit artifact output path when --output is supplied. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-setup validate-overlay-plan` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a passive setup overlay plan without setup apply, rollback execution, runtime start, model call, shell execution, Goose, deepagents, MCP/tool, or patch behavior. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-setup rollback-snapshot` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a passive setup rollback snapshot artifact by inspecting prior file states; runtime execution, model execution, shell execution, Goose runtime, deepagents runtime, MCP/tool invocation, patch authority, setup apply, and setup rollback execution are disabled. | Writes rollback snapshot JSON only to an explicit artifact output path when --output is supplied; it does not write target paths or secure prior-content stores. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-setup validate-rollback-snapshot` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates a passive setup rollback snapshot without rollback execution, setup apply, runtime start, model call, shell execution, Goose, deepagents, MCP/tool, or patch behavior. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-setup apply` | Tier 2 — operator-managed setup/runtime helper | `enabled` | Digest-bound governed setup write only; runtime execution, model execution, shell/subprocess execution, Goose runtime, deepagents runtime, MCP/tool invocation, patch authority, rollback execution, and B1 verification execution are disabled. | Writes only declared setup target paths from a validated overlay plan plus the explicit setup receipt output, and appends one ratification ledger line when a ratification store already exists; source writes are disabled except declared artifact-root setup metadata/config paths. | `explicit_operator_invocation` | Operator must approve the overlay_plan_digest before any mutation: --approve-digest matching overlay_plan_digest (scripted), an interactive typed digest-prefix confirmation when the flag is omitted, or a standing ratification grant the operator previously minted for setup.apply.overlay_digest. The receipt records which one applied in approval_mode (explicit_digest_bound_cli_flag, interactive_digest_prefix_confirmation, or standing_ratification_grant) and never conflates them; --rollback-snapshot must match the overlay. | `source_writes`, `artifact_writes` | `MUTATION_WITH_ROLLBACK_VERIFIED` | `allows_source_writes` is set |
| `builder-setup validate-receipt` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates setup apply receipts without setup apply, rollback execution, runtime start, model call, shell execution, Goose, deepagents, MCP/tool, or patch behavior. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-setup rollback` | Tier 2 — operator-managed setup/runtime helper | `enabled` | Digest-bound governed setup rollback execution only; B1 verification execution, B2 patch rollback, generic repository rollback, git rollback, shell/subprocess, runtime execution, model/provider, MCP/tool, Goose runtime, deepagents runtime, patch authority, and autonomous rollback are disabled. | Touches only changed_paths recorded in an applied setup receipt and covered by the supplied rollback snapshot; writes a setup rollback receipt only to explicit --output, and appends one ratification ledger line when a ratification store already exists. | `explicit_operator_invocation` | Operator must approve the setup receipt digest before any mutation: --approve-digest matching the receipt digest (scripted), an interactive typed digest-prefix confirmation when the flag is omitted, or a standing ratification grant the operator previously minted for setup.rollback.receipt_digest. The rollback receipt records which one applied in approval_mode (explicit_digest_bound_cli_flag, interactive_digest_prefix_confirmation, or standing_ratification_grant); the rollback snapshot must match receipt digests. | `source_writes`, `artifact_writes` | `MUTATION_WITH_ROLLBACK_VERIFIED` | `allows_source_writes` is set |
| `builder-setup validate-rollback-receipt` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates setup rollback receipts without setup apply, rollback execution, runtime start, model call, shell execution, Goose, deepagents, MCP/tool, git, generic rollback, B1, B2, or patch behavior. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-setup init` | Tier 1 — artifact-only planning/validation | `artifact_only` | Non-interactive governed onboarding wrapper generating passive setup plan, overlay plan, rollback snapshot, and intent report; no Goose, model, MCP, deepagents, patch, or shell execution. | Writes passive setup artifacts under explicit output directory only; no setup mutation or receipt generation. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-setup wizard` | Tier 1 — artifact-only planning/validation | `artifact_only` | Interactive governed onboarding wizard generating passive setup plan, overlay plan, rollback snapshot, and intent report; no Goose, model, MCP, deepagents, patch, or shell execution. | Writes passive setup artifacts under explicit output directory only; no setup mutation or receipt generation. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-setup validate-onboarding-intent` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates onboarding intent reports without setup apply, rollback execution, runtime start, model call, shell execution, Goose, deepagents, MCP/tool, git, or patch behavior. | No changes to workspace. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder workflow plan` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a passive governed workflow plan; no model, shell, Goose, deepagents, MCP, or target-repo execution. | Writes workflow session, profile, routing, orchestration, work-plan, event, replay, ledger, and status artifacts under the workflow output directory. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder workflow promote` | Tier 1 — artifact-only planning/validation | `artifact_only` | Records passive HITL promotion artifacts only; does not authorize execution. | Writes promotion request, review, decision, approval-boundary, transition, event, replay, ledger, and status artifacts. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder workflow candidate` | Tier 1 — artifact-only planning/validation | `artifact_only` | Records a passive execution candidate manifest without invoking commands. | Writes candidate manifest, validation report, transition, event, replay, ledger, and status artifacts. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder workflow verify-chain` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates artifact index and chain refs without running external tools. | Writes artifact index, chain verification, transition, event, replay, ledger, and status artifacts. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder workflow handoff` | Tier 1 — artifact-only planning/validation | `artifact_only` | Writes passive handoff summaries after chain verification; no runtime activation. | Writes handoff note, golden-path summary, demo README, transition, event, replay, ledger, and status artifacts. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder workflow status` | Tier 1 — artifact-only planning/validation | `validation_only` | Replays workflow events to reconstruct status without executing runtime work. | Writes refreshed replay, ledger, and status artifacts. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder ledger list` | Tier 1 — artifact-only planning/validation | `validation_only` | Reads workflow event ledgers and reconstructs status summaries only. | Read-only ledger inspection; no workspace changes. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder ledger replay` | Tier 1 — artifact-only planning/validation | `validation_only` | Replays event records deterministically without executing runtime work. | Writes replay report JSON to an explicit or default workflow artifact path. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder ledger audit` | Tier 1 — artifact-only planning/validation | `validation_only` | Scans event subject refs for a requested artifact SHA; no execution. | Read-only ledger inspection; no workspace changes. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder ledger query-receipts` | Tier 1 — artifact-only planning/validation | `validation_only` | Reads existing verification execution ledger records under .builder/ledger, validates records, and filters by receipt digest, chain digest, receipt status, or runner mode without replay, subprocess, shell, model, MCP, Goose, or deepagents runtime. | Read-only verification execution ledger inspection; no source, target repo, git, memory, patch, artifact, or B2 authority writes. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder ledger validate-receipts` | Tier 1 — artifact-only planning/validation | `validation_only` | Reads existing verification execution ledger records under .builder/ledger and emits a deterministic integrity report without replay execution, subprocess, shell, model, MCP, Goose, or deepagents runtime. | Read-only verification execution ledger integrity validation; no source, target repo, git, memory, patch, artifact, or B2 authority writes. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder ledger reconstruct-receipts` | Tier 1 — artifact-only planning/validation | `validation_only` | Reads existing verification execution ledger records under .builder/ledger and reconstructs passive receipt-chain projections without replay execution, subprocess, shell, model, MCP, Goose, or deepagents runtime. | Read-only verification execution ledger reconstruction; no source, target repo, git, memory, patch, artifact, or B2 authority writes. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder ledger export` | Tier 1 — artifact-only planning/validation | `artifact_only` | Exports event ledger artifacts without runtime execution. | Writes event ledger export JSON to an explicit or default workflow artifact path. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder ledger index-receipt` | Tier 1 — artifact-only planning/validation | `artifact_only` | Passively validates and indexes an existing B1.3 verification execution plan, approval, and receipt chain without subprocess, shell, model, MCP, Goose, deepagents, replay execution, patch behavior, git mutation, or source writes. | Writes one verification execution ledger record only under the target repo .builder/ledger directory. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-workflow plan` | Tier 1 — artifact-only planning/validation | `artifact_only` | Creates a passive governed workflow plan; no model, shell, Goose, deepagents, MCP, or target-repo execution. | Writes workflow session, profile, routing, orchestration, work-plan, event, replay, ledger, and status artifacts under the workflow output directory. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-workflow promote` | Tier 1 — artifact-only planning/validation | `artifact_only` | Records passive HITL promotion artifacts only; does not authorize execution. | Writes promotion request, review, decision, approval-boundary, transition, event, replay, ledger, and status artifacts. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-workflow candidate` | Tier 1 — artifact-only planning/validation | `artifact_only` | Records a passive execution candidate manifest without invoking commands. | Writes candidate manifest, validation report, transition, event, replay, ledger, and status artifacts. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-workflow verify-chain` | Tier 1 — artifact-only planning/validation | `validation_only` | Validates artifact index and chain refs without running external tools. | Writes artifact index, chain verification, transition, event, replay, ledger, and status artifacts. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-workflow handoff` | Tier 1 — artifact-only planning/validation | `artifact_only` | Writes passive handoff summaries after chain verification; no runtime activation. | Writes handoff note, golden-path summary, demo README, transition, event, replay, ledger, and status artifacts. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-workflow status` | Tier 1 — artifact-only planning/validation | `validation_only` | Replays workflow events to reconstruct status without executing runtime work. | Writes refreshed replay, ledger, and status artifacts. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-ledger list` | Tier 1 — artifact-only planning/validation | `validation_only` | Reads workflow event ledgers and reconstructs status summaries only. | Read-only ledger inspection; no workspace changes. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-ledger replay` | Tier 1 — artifact-only planning/validation | `validation_only` | Replays event records deterministically without executing runtime work. | Writes replay report JSON to an explicit or default workflow artifact path. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-ledger audit` | Tier 1 — artifact-only planning/validation | `validation_only` | Scans event subject refs for a requested artifact SHA; no execution. | Read-only ledger inspection; no workspace changes. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-ledger query-receipts` | Tier 1 — artifact-only planning/validation | `validation_only` | Reads existing verification execution ledger records under .builder/ledger, validates records, and filters by receipt digest, chain digest, receipt status, or runner mode without replay, subprocess, shell, model, MCP, Goose, or deepagents runtime. | Read-only verification execution ledger inspection; no source, target repo, git, memory, patch, artifact, or B2 authority writes. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-ledger validate-receipts` | Tier 1 — artifact-only planning/validation | `validation_only` | Reads existing verification execution ledger records under .builder/ledger and emits a deterministic integrity report without replay execution, subprocess, shell, model, MCP, Goose, or deepagents runtime. | Read-only verification execution ledger integrity validation; no source, target repo, git, memory, patch, artifact, or B2 authority writes. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-ledger reconstruct-receipts` | Tier 1 — artifact-only planning/validation | `validation_only` | Reads existing verification execution ledger records under .builder/ledger and reconstructs passive receipt-chain projections without replay execution, subprocess, shell, model, MCP, Goose, or deepagents runtime. | Read-only verification execution ledger reconstruction; no source, target repo, git, memory, patch, artifact, or B2 authority writes. | `none` | None. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-ledger export` | Tier 1 — artifact-only planning/validation | `artifact_only` | Exports event ledger artifacts without runtime execution. | Writes event ledger export JSON to an explicit or default workflow artifact path. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-ledger index-receipt` | Tier 1 — artifact-only planning/validation | `artifact_only` | Passively validates and indexes an existing B1.3 verification execution plan, approval, and receipt chain without subprocess, shell, model, MCP, Goose, deepagents, replay execution, patch behavior, git mutation, or source writes. | Writes one verification execution ledger record only under the target repo .builder/ledger directory. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-mcp` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Delegates governed MCP policy and execution subcommands. | No direct write authority at root CLI level. | `none` | Delegated to subcommands. | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-mcp inventory` | Tier 1 — artifact-only planning/validation | `artifact_only` | Outputs static passive MCP inventory artifact. | Writes artifact to specified path if output flag passed. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-mcp policy` | Tier 1 — artifact-only planning/validation | `artifact_only` | Validates or emits passive MCP policy artifact. | Writes artifact to specified path if output flag passed. | `none` | None. | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-mcp call` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Executes explicitly bounded low-risk MCP stub call from envelope. | Writes receipt and operational ledger events. | `explicit_operator_invocation` | Explicit envelope passing. | `artifact_writes`, `state_writes`, `external_tool_invocation` | `BOUNDED_EXECUTION_VERIFIED` | `allows_external_tool_invocation` is set |
| `builder-mcp standalone-call` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Executes explicitly bounded low-risk MCP stub call from envelope without logging to the workflow ledger. | Writes receipt to explicit output path only. | `explicit_operator_invocation` | Explicit envelope passing. | `artifact_writes`, `external_tool_invocation` | `BOUNDED_EXECUTION_VERIFIED` | `allows_external_tool_invocation` is set |
| `builder-mcp serve` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Runs a stdio JSON-RPC MCP server for Goose. Read-only stub tools run the governed envelope->receipt->ledger ceremony. Mutating tool classes are advertised but refused in-loop, deny-by-default (an mcp_call_denied event); the sole exception is propose_patch, which routes to the governed builder-hitl apply-patch lane ONLY when the operator has set BUILDER_MCP_GOVERNED_APPLY and supplies a schema-valid digest-bound approval, and even then apply_hitl_patch re-validates authority, binding, expiry, clean tree, and verification and fails closed. run_shell has no governed lane and always refuses. The server mints no write primitive and does not relax the read-only envelope schema. | Writes envelopes, receipts, and chained event records under .builder/sessions; the actual source write, if any, is performed by the delegated builder-hitl apply-patch lane under its own authority. | `explicit_operator_invocation` | Explicit operator invocation; read tool calls are envelope-bound; propose_patch additionally requires the BUILDER_MCP_GOVERNED_APPLY flag and a digest-bound hitl_patch_approval, verified inside apply_hitl_patch. | `artifact_writes`, `state_writes`, `external_tool_invocation` | `BOUNDED_EXECUTION_VERIFIED` | `allows_external_tool_invocation` is set |
| `builder-tools invoke` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Executes explicitly bounded low-risk tool stub call from envelope. | Writes receipt and operational ledger events. | `explicit_operator_invocation` | Explicit envelope passing. | `artifact_writes`, `state_writes`, `external_tool_invocation` | `BOUNDED_EXECUTION_VERIFIED` | `allows_external_tool_invocation` is set |
| `builder-tools standalone-invoke` | Tier 3 — HITL-gated execution candidate | `hitl_runtime_candidate` | Executes explicitly bounded low-risk tool stub call from envelope without logging to the workflow ledger. | Writes receipt to explicit output path only. | `explicit_operator_invocation` | Explicit envelope passing. | `artifact_writes`, `external_tool_invocation` | `BOUNDED_EXECUTION_VERIFIED` | `allows_external_tool_invocation` is set |

## Synthesized records (117)

Nobody declared these commands. Each one's authority is *inherited* — copied from the
nearest declared ancestor of its name, on a word boundary. That ancestor is always a
command group, because a record becomes a group by acquiring subcommands: all 117 of
these rows state a classification that describes their parent and was never checked
against them.

So `check_command_authority` refuses to certify a requested effect for an inherited
record, whatever its `Capabilities` column says. The classification below is reported,
not spendable. Promoting one of these means declaring it directly — with evidence — not
editing its parent.

| Command Name | Inherits Authority From | Tier | State | Capabilities | Assurance | Assurance Derived From |
|---|---|---|---|---|---|---|
| `builder-code-vault frame/recall` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-platform tui` | `builder-platform` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder tui status` | `builder tui` (command group) | Tier 0 — read-only inspection | `spec_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder tui roster` | `builder tui` (command group) | Tier 0 — read-only inspection | `spec_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder tui gates` | `builder tui` (command group) | Tier 0 — read-only inspection | `spec_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder tui hitl` | `builder tui` (command group) | Tier 0 — read-only inspection | `spec_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder tui handoff` | `builder tui` (command group) | Tier 0 — read-only inspection | `spec_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder tui golden` | `builder tui` (command group) | Tier 0 — read-only inspection | `spec_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-lanes list` | `builder-lanes` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-lanes show` | `builder-lanes` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-context validate` | `builder-context` (command group) | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-context summarize` | `builder-context` (command group) | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-git-state artifact` | `builder-git-state` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes`, `readonly_subprocess` | `BOUNDED_EXECUTION_VERIFIED` | `allows_readonly_subprocess` is set |
| `builder-git-state identity` | `builder-git-state` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes`, `readonly_subprocess` | `BOUNDED_EXECUTION_VERIFIED` | `allows_readonly_subprocess` is set |
| `builder-git-state validate` | `builder-git-state` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes`, `readonly_subprocess` | `BOUNDED_EXECUTION_VERIFIED` | `allows_readonly_subprocess` is set |
| `builder-session plan` | `builder-session` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-session validate` | `builder-session` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-session config` | `builder-session` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-session validate-config` | `builder-session` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-session goose-projection` | `builder-session` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-session validate-goose-projection` | `builder-session` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-session goose-wrapper-plan` | `builder-session` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-session validate-goose-wrapper-plan` | `builder-session` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-session goose-readonly-plan` | `builder-session` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-session validate-goose-readonly-plan` | `builder-session` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-session repo-map` | `builder-session` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-session context-pack` | `builder-session` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault frame` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault digest` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault extractor-manifest` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault validate-extractor-manifest` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault lint` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault context` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault recall` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault validate-frame` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault validate-lint` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault validate-context` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault validate-recall` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault structural-field` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault validate-structural-field` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault demo` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `DEMO_ONLY_VERIFIED` | the command name says `demo` |
| `builder-code-vault validate-demo` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `DEMO_ONLY_VERIFIED` | the command name says `demo` |
| `builder-code-vault bench` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault validate-bench` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault corroborate` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault validate-corroboration` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault utility-registry` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault validate-utility-registry` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault utility-baseline` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault validate-utility-eval` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault relation-field` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault change-field` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault evidence-relation` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault reconstruction` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault validate-relation-field` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault validate-evidence-relation` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault validate-reconstruction` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-code-vault validate-change-field` | `builder-code-vault` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-session operator-surface` | `builder-session` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-session command-surface` | `builder-session` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-agent profiles` | `builder-agent` (command group) | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-agent show` | `builder-agent` (command group) | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-agent render` | `builder-agent` (command group) | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-agent validate` | `builder-agent` (command group) | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-agent artifact` | `builder-agent` (command group) | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-bridge doctor` | `builder-bridge` (command group) | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-bridge deepagents-smoke` | `builder-bridge` (command group) | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-bridge render` | `builder-bridge` (command group) | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-bridge validate-artifact` | `builder-bridge` (command group) | Tier 2 — operator-managed setup/runtime helper | `operator_managed` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-bundle create` | `builder-bundle` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-bundle validate` | `builder-bundle` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-goose propose-command` | `builder-goose` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-goose validate-command-proposal` | `builder-goose` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-goose env` | `builder-goose` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-records record` | `builder-records` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-records validate` | `builder-records` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-preflight record` | `builder-preflight` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-preflight validate` | `builder-preflight` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-receipt record` | `builder-receipt` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-receipt validate` | `builder-receipt` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-chain record` | `builder-chain` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-chain validate` | `builder-chain` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-chain verify-artifacts` | `builder-chain` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-handoff record` | `builder-handoff` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-handoff validate` | `builder-handoff` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-intake record` | `builder-intake` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-intake validate` | `builder-intake` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-index record` | `builder-index` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-index validate` | `builder-index` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-promotion record` | `builder-promotion` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-promotion validate` | `builder-promotion` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-promotion-decision record` | `builder-promotion-decision` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-promotion-decision validate` | `builder-promotion-decision` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-state-index record` | `builder-state-index` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-state-index validate` | `builder-state-index` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-snapshot record` | `builder-snapshot` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-snapshot validate` | `builder-snapshot` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-notes handoff` | `builder-notes` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-notes validate` | `builder-notes` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-quality plan` | `builder-quality` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-quality validate` | `builder-quality` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-research profiles` | `builder-research` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-research show` | `builder-research` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-research validate-profiles` | `builder-research` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-research plan` | `builder-research` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-research validate` | `builder-research` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-research adapter` | `builder-research` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-research validate-adapter` | `builder-research` (command group) | Tier 1 — artifact-only planning/validation | `artifact_only` | `artifact_writes` | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-performance record` | `builder-performance` (command group) | Tier 0 — read-only inspection | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-performance validate` | `builder-performance` (command group) | Tier 0 — read-only inspection | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-performance benchmark-validation` | `builder-performance` (command group) | Tier 0 — read-only inspection | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-performance parity-report` | `builder-performance` (command group) | Tier 0 — read-only inspection | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-readonly report` | `builder-readonly` (command group) | Tier 0 — read-only inspection | `read_only_runtime_candidate` | — | `READ_ONLY_RUNTIME_VERIFIED` | promotion state is `read_only_runtime_candidate` |
| `builder-verification list` | `builder-verification` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-verification show` | `builder-verification` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-verification artifact` | `builder-verification` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
| `builder-verification validate` | `builder-verification` (command group) | Tier 1 — artifact-only planning/validation | `validation_only` | — | `PASSIVE_ARTIFACT_VERIFIED` | no flag or state raises assurance above passive |
