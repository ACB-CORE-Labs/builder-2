# builder-II roadmap (updated post-PR#27 + bundle)

builder-II is the generic governed local agent/developer platform.

## Current status: 100% on governance foundation plan

- Generic platform core, target profiles, agent profiles, bridge specs, git preflight/postflight, artifact bundling: **DONE**.
- Remaining surface work (quality gates, notes vault, evals, read-only runner, HITL flows) can now be added incrementally as thin extensions on top of the stable bundle + target + profile system.
- CORE remains a target profile only.

The plan is complete at the architecture/governance level. The platform is now ready for safe, reviewable extension to full HITL-gated execution when desired.

See bundle command for consolidated handoff artifacts.
