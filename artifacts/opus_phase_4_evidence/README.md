# Phase 4 evidence bundle

Regenerate every measurement in `../opus_phase_4_verification_report.md`:

    bash artifacts/opus_phase_4_evidence/reproduce.sh

| File | What it is |
|---|---|
| `reproduce.sh` | Regenerates §1–§4 from a clean tree. Mutates `app.py` and restores it. |
| `gate_receipt.json` | `builder_ii.gate_battery_receipt`, `capability_state: RECORDED_ONLY`. Validate: `uv run python -m builder_ii.gate_battery_receipt --validate <path>` |
| `palette_dom_before_fix.json` | Semantic DOM at `2990a59`: 463 entries, **0** `⚡`. Not regenerable once the fix is merged — this is the recorded defect. |
| `palette_dom_after_fix.json` | Semantic DOM after the fix: 463 entries, **18** `⚡`. |

The raw `tui_audit_ledger.jsonl` is deliberately absent: it is a runtime artifact written to
gitignored `.builder/` and regenerated by `reproduce.sh`. Validate one with
`uv run python scripts/validate_tui_audit_ledger.py <path>`.

**This bundle is not proof.** The receipt's own governance block pins `independent_observer:
false` and `artifact_is_authority: false` — the same host that ran the gates wrote it. It closes
transcription error and dirty-tree ambiguity, not dishonesty. The load-bearing evidence is
`reproduce.sh` §C, which is adversarial: it shows the repository's own pre-existing test passing
on broken code.
