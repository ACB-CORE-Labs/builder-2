# Governor — mastery/S2-v2 gateway nodes

## G-LEAD focus
1. v2 does not silently enable cloud provider model calls (`cloud_provider_invoke=false`)
2. Default `gateway_mode=record` is MSDA + synthetic receipts only
3. `stub_tool` limited to B7 allowlist; model_gateway refuses stub_tool
4. v1 still refuses gateway flags/nodes
5. CAPABILITY_PROMOTION / COMMAND_AUTHORITY match real power (HITL candidate, not S3)

Write: `artifacts/wrp_exchange/mastery/S2-v2/governor/wave_mastery_S2_v2_cert.json`
